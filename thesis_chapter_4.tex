\chapter{\babEmpat}
\label{bab:4}
In this chapter, we will explain the detailed implementation and the libraries or resources we utilize.

\section{Dataset Generation Implementation}

Our dataset generation system is implemented as a modular, extensible framework that supports both template-based and random-walk approaches to create comprehensive NL2SPARQL datasets. This section details the technical implementation of each component, their interactions, and domain-specific adaptations for the university curriculum, legal, and GESIS knowledge graphs.

\subsection{System Architecture Overview}

The dataset generation system consists of five main components that work together to produce high-quality NL2SPARQL datasets:

\begin{enumerate}
    \item \textbf{Knowledge Graph Schema Extractor}: Analyzes the knowledge graph to identify classes, properties, entity examples, and type relationships
    
    \item \textbf{Vector Search Engine}: Provides semantic search capabilities for entity and property matching using Weaviate and sentence embeddings
    
    \item \textbf{Template-Based Generator}: Creates linguistically diverse questions with corresponding SPARQL queries using a discovery-first approach
    
    \item \textbf{Random-Walk Generator}: Discovers structural patterns in the knowledge graph to ensure comprehensive coverage of query patterns
    
    \item \textbf{Post-Processing Pipeline}: Standardizes SPARQL query formats, enhances dataset with LLM-generated content, and prepares final training/testing splits
\end{enumerate}

The implementation follows a modular design pattern where each component is implemented as a separate Python class with well-defined interfaces, enabling flexible combination and domain-specific customization.

\subsection{Knowledge Graph Schema Extraction}

The foundation of our dataset generation system is the knowledge graph schema extractor, which provides a unified interface for extracting schema information from different knowledge graph sources, including remote SPARQL endpoints and local RDF files.

\begin{figure}
    \centering
    \includegraphics[width=0.65\linewidth]{kgschema_41.jpg}
    \caption{Knowledge Graph Schema Extraction Pipeline}
    \label{fig:enter-label}
\end{figure}

\subsubsection{Unified Schema Extraction Interface}

We implemented the schema extraction interface through the \texttt{KGSchemaExtractor} class, which provides multiple extraction methods to handle different knowledge graph sources. The complete implementation is shown in \hyperref[code:schema-extractor-interface]{Appendix~\ref{sec:implementation-code}}.

\subsubsection{Domain-Specific Schema Processing}

For each knowledge graph domain, we implemented specialized parsers that handle domain-specific schema characteristics. The implementation details for the university course graph parsing are provided in \hyperref[code:domain-schema-processing]{Appendix~\ref{sec:implementation-code}}.

The GESIS and Legal schema processors follow similar patterns but with domain-specific property categorization and label extraction logic.

\subsection{Query Optimization for Large Knowledge Graphs}
\label{query-optimization}

A critical aspect of our dataset generation implementation is the optimization of SPARQL queries, particularly when working with large knowledge graphs such as GESIS KG. Query performance can significantly impact the efficiency of the dataset generation process, especially during the discovery-first instantiation phase where multiple queries need to be executed to find valid entity-property combinations.

\subsubsection{Triple Pattern Ordering}

We discovered that the order of triple patterns within SPARQL queries can dramatically affect execution time. Consider the following example from our work with the GESIS Knowledge Graph (before modification):

\begin{lstlisting}[language=SPARQL, caption=Original query - execution time: 2.155 seconds]
SELECT ?authorName WHERE {
  ?author schema:name ?authorName .
  ?publication schema:author ?author .
}
GROUP BY ?author ?authorName
ORDER BY DESC(COUNT(?publication))
LIMIT 1
\end{lstlisting}

By simply reordering the triple patterns, we achieved a significant performance improvement:

\begin{lstlisting}[language=SPARQL, caption=Reordered query - execution time: 0.698 seconds]
SELECT ?authorName WHERE {
  ?publication schema:author ?author .
  ?author schema:name ?authorName .
}
GROUP BY ?author ?authorName
ORDER BY DESC(COUNT(?publication))
LIMIT 1
\end{lstlisting}

This performance difference occurs because of how SPARQL query execution engines process the patterns. In the first query, \texttt{?author schema:name ?authorName} instantiates all entities with a name property, including many that aren't actually authors. This creates a large intermediate result set that must then be filtered by the second triple pattern.

In contrast, the reordered query first identifies publications and their authors with \texttt{?publication schema:author ?author}, creating a much smaller intermediate result set of only actual authors, before retrieving their names. This pattern ordering ensures that we don't instantiate unnecessary triples during query execution.

\subsubsection{Query Structure Optimization}

We implemented further optimizations by restructuring queries to minimize redundant operations. For example, we can enhance the previous query by moving the \texttt{schema:name} property retrieval outside of the grouping operation:

\begin{lstlisting}[language=SPARQL, caption=Fully optimized query with nested structure]
PREFIX schema: <https://schema.org/>
PREFIX ns1: <http://example.org/>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX lex2kg-o: <https://example.org/lex2kg/ontology/>
PREFIX gesiskg: <https://data.gesis.org/gesiskg/schema/>
SELECT ?authorName WHERE {
  {
    SELECT ?author WHERE {
      ?publication schema:author ?author .
    }
    GROUP BY ?author
    ORDER BY DESC(COUNT(?publication))
    LIMIT 1
  }
  ?author schema:name ?authorName .
}
\end{lstlisting}

This structure ensures that we only retrieve the name for the single author with the largest number of publications, rather than retrieving names for all authors before filtering. This optimization is particularly valuable when working with large knowledge graphs containing millions of entities.

\subsubsection{Validation and Performance Monitoring}

To systematically identify and address performance issues in our query templates, we implemented a comprehensive query validation and execution monitoring system. Rather than relying on an automated optimization function, we developed a manual optimization workflow using our \texttt{validate\_gesis\_sparql.py} script:

\begin{lstlisting}[language=python, caption=Query validation and performance monitoring implementation]
def validate_and_execute_sparql_queries(input_json_path, output_json_path=None):
    """Validates and executes SPARQL queries against the Fuseki server."""
    # Initialize the SPARQL executor
    sparql_exec = SparqlExecutor()

    # Read the input JSON file with queries
    with open(input_json_path, "r", encoding="utf-8") as f:
        data = json.load(f)

    # Process each query
    for i, item in enumerate(data):
        query_id = item.get("id", f"query_{i+1}")
        sparql = item.get("sparql", "")
        
        try:
            # Execute the SPARQL query
            query_results = sparql_exec.execute_query(sparql, return_format="dict")
            
            # Flag slow queries for manual optimization
            if execution_time > 2.0:
                # Mark for optimization
                pass
        except Exception as e:
            # Handle errors in SPARQL execution
            pass
\end{lstlisting}

This script allows us to run all generated queries against the target knowledge graph and identify those that exceed our performance threshold (typically 2 seconds). When slow queries are identified, we manually analyze them to determine the cause of the performance issue and apply appropriate optimizations:

\begin{enumerate}
    \item Reordering triple patterns to prioritize selective patterns first
    \item Restructuring queries with nested subqueries to optimize operations
    \item Removing unnecessary graph patterns that don't contribute to the result
    \item Adding explicit type constraints to limit intermediate result size
\end{enumerate}

The manual optimization approach, while more labor-intensive than an automated system, ensures that each template in our dataset generation system produces efficient queries that can be executed reliably against large knowledge graphs. This was particularly important for the GESIS Knowledge Graph, which contains millions of triples describing scholarly publications, authors, and related metadata.

By systematically identifying and optimizing slow-performing queries, we significantly improved the overall efficiency of our dataset generation process, reducing the average query execution time by approximately 60\% across our template library.

\subsection{Vector Search Integration}

To enable efficient entity and property matching, we integrated Weaviate vector search capabilities with n-gram processing and sentence embeddings. This integration is critical for both template-based and random-walk approaches.

\begin{figure}
    \centering
    \includegraphics[width=0.8\linewidth]{assets/pics/ngram_coba_lagi.jpg}
    \caption{N-gram Based Entity and Property Matching System}
    \label{fig:enter-label}
\end{figure}

\subsubsection{N-gram Processing Implementation}

We implemented n-gram processing to break down natural language questions into searchable chunks, enabling more effective matching with knowledge graph elements. The complete implementation is provided in \hyperref[code:ngram-processing]{Appendix~\ref{sec:implementation-code}}.

\subsubsection{Weaviate Vector Search Implementation}

The vector search implementation uses Weaviate collections to store and retrieve entity and property embeddings. This approach is shared across both template-based and random-walk generators. The complete implementation is shown in \hyperref[code:weaviate-search]{Appendix~\ref{sec:implementation-code}}.

\subsubsection{Entity and Property Matching}

The entity and property matching system combines n-gram processing with vector search to identify which entities and properties are mentioned in natural language questions. The complete implementation is provided in \hyperref[code:entity-property-matching]{Appendix~\ref{sec:implementation-code}}.

This integrated approach for entity and property matching is used by both the template-based and random-walk generators, ensuring consistent matching behavior across the entire system.

\subsection{Template-Based Dataset Generation}

The template-based generator creates diverse question-SPARQL pairs by defining question templates with placeholders that are instantiated with valid entity-property combinations discovered from the knowledge graph.

\begin{figure}
    \centering
    \includegraphics[width=0.8\linewidth]{assets/pics/discovery-new.jpg}
    \caption{Discovery-First Template Instantiation Process}
    \label{fig:enter-label}
\end{figure}

\subsubsection{Template Structure and Initialization}

Templates are structured as dictionaries with fields for question variations, SPARQL patterns, reasoning chains, and metadata. The complete template structure implementation is provided in \hyperref[code:template-structure]{Appendix~\ref{sec:implementation-code}}.

\subsubsection{Discovery-First Template Instantiation}

The key innovation in our implementation is the discovery-first approach that guarantees all generated question-SPARQL pairs are valid and executable. The complete implementation is shown in \hyperref[code:discovery-first]{Appendix~\ref{sec:implementation-code}}.

\subsubsection{Chain of Thoughts Generation}

Our implementation includes the generation of step-by-step reasoning chains that explain the translation from natural language to SPARQL:

\begin{lstlisting}[breaklines=true,language=python, caption=Chain of Thoughts Generation, label=code:chain-of-thoughts]
def generate_chain_of_thoughts(self, question, sparql, template):
    """
    Generate a chain of thoughts explaining how to translate the question to SPARQL
    
    Args:
        question (str): Natural language question
        sparql (str): SPARQL query
        template (dict): Template used to generate the question-query pair
        
    Returns:
        list: List of thought steps
    """
    if "thoughtsTemplate" not in template:
        # Fallback for templates without thoughtsTemplate
        return self.generate_default_thoughts()
    
    # Get the thoughts template
    thoughts_template = template["thoughtsTemplate"]
    
    # Extract entity and property URIs from SPARQL
    entity_uris, property_uris = self._extract_uris_from_sparql(sparql)
    
    # Create mappings for replacement
    all_mappings = self._create_replacement_mappings(entity_uris, property_uris, sparql)
    
    # Replace placeholders in thoughts
    processed_thoughts = []
    for thought in thoughts_template:
        processed_thought = thought
        
        # Replace each placeholder with the appropriate value
        for placeholder, mapping in all_mappings.items():
            pattern = r'\{' + re.escape(placeholder) + r'\}'
            replacement_value = self.get_appropriate_replacement(thought, placeholder, mapping)
            processed_thought = re.sub(pattern, replacement_value, processed_thought)
        
        # Handle special case for the first entity
        if 'entity' in all_mappings and '{entity}' not in processed_thought and '{entity1}' in processed_thought:
            entity_mapping = all_mappings['entity']
            processed_thought = re.sub(r'\{entity1\}', self.get_appropriate_replacement(thought, 'entity1', entity_mapping), processed_thought)
        
        processed_thoughts.append(processed_thought)
    
    return processed_thoughts

def get_appropriate_replacement(self, thought_text, placeholder, mapping):
    """
    Determine whether to use URI or label based on the context in the thought
    """
    # Check context to determine appropriate replacement
    thought_lower = thought_text.lower()
    
    # Contexts where URI/prefixed form is more appropriate
    if any(phrase in thought_lower for phrase in [
        "in the ontology",
        "represents the",
        "ns1:",
        "property '",
        "entity '",
        "via the '",
        "using",
        "through"
    ]):
        return mapping.get('prefixed', mapping.get('uri', mapping.get('label', placeholder)))
    
    # Contexts where label form is more appropriate
    elif any(phrase in thought_lower for phrase in [
        "categorized as",
        "belonging to", 
        "classified as",
        "of the '",
        "as a '",
        "category '",
        "group '",
        "method '",
        "course '"
    ]):
        return mapping.get('label', mapping.get('value', placeholder))
    
    # Default to label for most contexts
    return mapping.get('label', mapping.get('value', placeholder))
\end{lstlisting}

\subsection{Random-Walk Dataset Generation}

The random-walk generator complements the template-based approach by discovering valid query patterns directly from the knowledge graph structure, ensuring comprehensive coverage of possible query patterns.

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{assets/pics/rw-gen-new.jpg}
    \caption{Random-Walk Pattern Discovery and Question Generation}
    \label{fig:enter-label}
\end{figure}

\subsubsection{Pattern Discovery Implementation}

We implemented a pattern discovery system that traverses the knowledge graph to find valid patterns of varying complexity. The complete implementation is provided in \hyperref[code:pattern-discovery]{Appendix~\ref{sec:implementation-code}}.

We implemented similar pattern discovery methods for 2-property and 3-property patterns, increasing complexity with each level:

\begin{lstlisting}[breaklines=true,language=python, caption=Multi-property Pattern Discovery, label=code:multi-property-patterns]
def generate_2_property_patterns(self, count=100):
    """Generate 2-property patterns using discovery-first approach"""
    # Discovery query for middle target pattern: entity1 prop1 ?target . ?target prop2 entity2
    middle_discovery_query = """
        SELECT DISTINCT ?prop1 ?prop2 ?entity1 ?entity2 ?middle WHERE {
            ?entity1 ?prop1 ?middle .
            ?middle ?prop2 ?entity2 .
            FILTER(STRSTARTS(STR(?prop1), "http://example.org/"))
            FILTER(STRSTARTS(STR(?prop2), "http://example.org/"))
            FILTER(STRSTARTS(STR(?entity1), "http://example.org/"))
            FILTER(STRSTARTS(STR(?entity2), "http://example.org/"))
            FILTER(?prop1 != ?prop2)
        }
    """
    
    # Discovery query for branching pattern: ?target prop1 ?hidden . ?hidden prop2 entity
    branching_discovery_query = """
        SELECT DISTINCT ?prop1 ?prop2 ?entity WHERE {
            ?target ?prop1 ?hidden .
            ?hidden ?prop2 ?entity .
            FILTER(STRSTARTS(STR(?prop1), "http://example.org/"))
            FILTER(STRSTARTS(STR(?prop2), "http://example.org/"))
            FILTER(STRSTARTS(STR(?entity), "http://example.org/"))
            FILTER(?prop1 != ?prop2)
        }
    """
    
    # ... [implementation for generating 2-property patterns] ...

def generate_3_property_patterns(self, count=100):
    """Generate 3-property patterns using discovery-first approach"""
    # Discovery query for linear end pattern: entity prop1 ?h1 . ?h1 prop2 ?h2 . ?h2 prop3 ?target
    linear_end_query = """
        SELECT DISTINCT ?prop1 ?prop2 ?prop3 ?entity WHERE {
            ?entity ?prop1 ?h1 .
            ?h1 ?prop2 ?h2 .
            ?h2 ?prop3 ?target .
            FILTER(STRSTARTS(STR(?prop1), "http://example.org/"))
            FILTER(STRSTARTS(STR(?prop2), "http://example.org/"))
            FILTER(STRSTARTS(STR(?prop3), "http://example.org/"))
            FILTER(STRSTARTS(STR(?entity), "http://example.org/"))
            FILTER(?prop1 != ?prop2 && ?prop2 != ?prop3 && ?prop1 != ?prop3)
        }
    """
    
    # ... [additional discovery queries and implementation for 3-property patterns] ...
\end{lstlisting}

\subsubsection{Pattern Variation Generation}

To ensure diversity, we implemented a systematic pattern variation generator that creates multiple variations of each base pattern by reversing triple directions:

\begin{lstlisting}[breaklines=true,language=python, caption=Pattern Variation Generation, label=code:pattern-variations]
def _create_linear_end_pattern(self, data, pattern_index):
    """
    Create linear end pattern from discovery data with variations
    
    Args:
        data: Discovery data with properties and entity
        pattern_index: Index for pattern identification
        
    Returns:
        dict: Pattern information or None if invalid
    """
    prop1, prop2, prop3, entity = data
    
    props_str = [self._shorten_uri(p) for p in [prop1, prop2, prop3]]
    entity_str = self._shorten_uri(entity)
    
    # Generate random variation (8 possibilities using bit manipulation)
    variation = random.randint(0, 7)
    
    pattern_parts = []
    
    # Determine direction of each triple based on bit pattern
    if variation & 1:  # bit 0: reverse first triple
        pattern_parts.append(f"?hidden1 {props_str[0]} {entity_str}")
    else:
        pattern_parts.append(f"{entity_str} {props_str[0]} ?hidden1")
        
    if variation & 2:  # bit 1: reverse second triple
        pattern_parts.append(f"?hidden2 {props_str[1]} ?hidden1")
    else:
        pattern_parts.append(f"?hidden1 {props_str[1]} ?hidden2")
        
    if variation & 4:  # bit 2: reverse third triple
        pattern_parts.append(f"?target {props_str[2]} ?hidden2")
    else:
        pattern_parts.append(f"?hidden2 {props_str[2]} ?target")
        
    pattern = " . ".join(pattern_parts)
    sparql = f"SELECT ?target WHERE {{ {pattern} . }}"
    
    # Validate pattern has results
    if self._validate_pattern(sparql):
        return {
            'id': f'3p_linear_end_{variation}_{pattern_index}',
            'sparql': self._format_sparql(sparql),
            'pattern_type': f'3_prop_linear_end_v{variation+1}',
            'complexity': 'advanced',
            'properties': props_str,
            'fixed_entity': entity_str
        }
    return None
\end{lstlisting}

\subsubsection{LLM-Assisted Question and Reasoning Generation}

After generating SPARQL queries through pattern discovery, we use Large Language Models to generate natural language questions and reasoning chains. To ensure consistency in question style and reasoning approach across both generation methods, our implementation leverages example outputs from the template-based generation as part of the prompt context. We implemented a specialized prompt template for this purpose:

\begin{tcolorbox}[title=SPARQL to Question and Thoughts Generation Prompt, colback=blue!5!white, colframe=blue!75!black, boxrule=0.5mm, arc=2mm, fontupper=\ttfamily, verbatim, breakable, before upper=\vspace{2mm}]
\small
\textbf{\#\# INSTRUCTIONS} \\
You are a SPARQL question generation expert. Your task is to generate a natural language question and step-by-step reasoning chain for the given SPARQL query.\\

\textbf{\#\# SPARQL QUERY} \\
\{sparql\_query\}\\

\textbf{\#\# ENTITY AND PROPERTY INFORMATION} \\
- Entities: \{entity\_info\}\\
- Properties: \{property\_info\}\\

\textbf{\#\# EXAMPLES FROM TEMPLATE-BASED GENERATION} \\
Here are examples of questions and reasoning chains from our template-based generation system:

Example 1:
\{example\_question\_1\}
\{example\_thoughts\_1\}

Example 2:
\{example\_question\_2\}
\{example\_thoughts\_2\}

\textbf{\#\# OUTPUT REQUIREMENTS} \\
Please generate:\\
1. A clear, concise natural language question that would be answered by this SPARQL query\\
2. A step-by-step reasoning chain (5-7 steps) that explains how to translate the natural language question into the SPARQL query\\

\textbf{\#\# OUTPUT FORMAT} \\
```json
\{
  "question": "Your generated natural language question here",
  "thoughts": [
    "1. First reasoning step...",
    "2. Second reasoning step...",
    "3. Third reasoning step...",
    "..."
  ]
\}
```\\

Make sure the question accurately represents what the SPARQL query is asking, and the reasoning steps provide a clear explanation of how to translate from the question to the SPARQL. Follow the style and format of the provided examples.
\end{tcolorbox}

The implementation for generating and processing LLM-assisted content is shown below:

\begin{lstlisting}[breaklines=true,language=python, caption=LLM-Assisted Content Generation, label=code:llm-assisted-generation]
def generate_llm_content(patterns, template_examples, output_path):
    """
    Generate natural language questions and reasoning chains using LLM
    
    Args:
        patterns (list): List of patterns with SPARQL queries
        template_examples (list): Examples from template-based generation for context
        output_path (str): Path to save LLM-generated content
    """
    llm_inputs = []
    
    # Select representative examples from template-based generation
    selected_examples = select_diverse_examples(template_examples, num_examples=3)
    
    # Prepare inputs for LLM
    for pattern in patterns:
        # Extract entity and property information
        entity_uris, property_uris = extract_uris_from_sparql(pattern['sparql'])
        
        entity_info = []
        for uri in entity_uris:
            label = get_label_for_uri(uri)
            entity_info.append(f"{label} ({uri})")
        
        property_info = []
        for uri in property_uris:
            label = get_label_for_uri(uri)
            property_info.append(f"{label} ({uri})")
        
        # Format template examples for context
        example_context = format_examples_for_prompt(selected_examples)
        
        # Create prompt for LLM
        prompt = f"""
        You are a SPARQL question generation expert. Your task is to generate a natural language question 
        and step-by-step reasoning chain for the given SPARQL query.

        SPARQL Query:
        {pattern['sparql']}

        Entity and Property Information:
        - Entities: {', '.join(entity_info)}
        - Properties: {', '.join(property_info)}
        
        Examples from Template-Based Generation:
        {example_context}

        Please generate:
        1. A clear, concise natural language question that would be answered by this SPARQL query
        2. A step-by-step reasoning chain (5-7 steps) that explains how to translate the natural 
           language question into the SPARQL query

        Output Format:
        ```json
        {{
          "question": "Your generated natural language question here",
          "thoughts": [
            "1. First reasoning step...",
            "2. Second reasoning step...",
            "3. Third reasoning step...",
            "..."
          ]
        }}
        ```
        
        Follow the style and format of the provided examples.
        """
        
        llm_inputs.append({
            'id': pattern['id'],
            'prompt': prompt
        })
    
    # Generate content using LLM (e.g., via API or local model)
    llm_outputs = call_llm_with_prompts(llm_inputs)
    
    # Process and save LLM outputs
    processed_outputs = []
    for output in llm_outputs:
        try:
            # Extract JSON from the LLM response
            json_match = re.search(r'```json\s*(.*?)\s*```', output['response'], re.DOTALL)
            if json_match:
                json_str = json_match.group(1)
                content = json.loads(json_str)
                
                processed_outputs.append({
                    'id': output['id'],
                    'question': content['question'],
                    'thoughts': content['thoughts']
                })
            else:
                print(f"No JSON found in response for {output['id']}")
        except Exception as e:
            print(f"Error processing LLM output for {output['id']}: {e}")
    
    # Save processed outputs
    with open(output_path, 'w') as f:
        json.dump(processed_outputs, f, indent=2)
    
    return processed_outputs

def select_diverse_examples(template_examples, num_examples=3):
    """
    Select diverse examples from template-based generation for context
    
    Args:
        template_examples (list): Examples from template-based generation
        num_examples (int): Number of examples to select
        
    Returns:
        list: Selected diverse examples
    """
    # Group examples by complexity
    complexity_groups = {}
    for example in template_examples:
        complexity = example.get('complexity', 'basic')
        if complexity not in complexity_groups:
            complexity_groups[complexity] = []
        complexity_groups[complexity].append(example)
    
    # Select examples from each complexity group if available
    selected = []
    for complexity in ['basic', 'intermediate', 'advanced']:
        if complexity in complexity_groups and complexity_groups[complexity]:
            selected.append(random.choice(complexity_groups[complexity]))
    
    # If we need more examples, add random ones
    if len(selected) < num_examples:
        remaining = template_examples.copy()
        for example in selected:
            if example in remaining:
                remaining.remove(example)
        
        additional = random.sample(
            remaining, 
            min(num_examples - len(selected), len(remaining))
        )
        selected.extend(additional)
    
    return selected[:num_examples]

def format_examples_for_prompt(examples):
    """
    Format template examples for inclusion in the prompt
    
    Args:
        examples (list): Selected examples from template-based generation
        
    Returns:
        str: Formatted examples for prompt context
    """
    formatted = []
    for i, example in enumerate(examples):
        example_text = f"Example {i+1}:\n"
        example_text += f"Question: {example.get('question', '')}\n"
        example_text += "Reasoning chain:\n"
        
        thoughts = example.get('thoughts', [])
        for thought in thoughts:
            example_text += f"  {thought}\n"
        
        formatted.append(example_text)
    
    return "\n".join(formatted)
\end{lstlisting}

\subsection{Domain-Specific Customizations}

Each knowledge graph domain (university curriculum, legal, and GESIS) has specialized components that handle domain-specific patterns and properties.

\subsubsection{Curriculum KG Customizations}

For the Curriculum Knowledge Graph, we implemented specialized templates and property categorization to handle academic course information:

\begin{lstlisting}[breaklines=true,language=python, caption=Curriculum KG Customizations, label=code:curriculum-customizations]
# Curriculum-specific configuration
curriculum_config = {
    "prefixes": {
        "ns1": "http://example.org/",
        "rdfs": "http://www.w3.org/2000/01/rdf-schema#",
        "xsd": "http://www.w3.org/2001/XMLSchema#"
    },
    "entity_classes": [
        "http://example.org/course",
        "http://example.org/evaluation",
        "http://example.org/course_category",
        "http://example.org/research_lab"
    ],
    "primary_entity_class": "http://example.org/course",
    "property_categorization": {
        "numeric_properties": [
            "http://example.org/has_credits",
            "http://example.org/has_minimum_credits"
        ],
        "text_properties": [
            "http://example.org/has_course_code",
            "http://example.org/also_known_as"
        ],
        "object_properties": [
            "http://example.org/has_course_category",
            "http://example.org/has_prerequisite_course",
            "http://example.org/has_evaluation_method",
            "http://example.org/has_research_group"
        ]
    },
    "excluded_properties": [
        "http://www.w3.org/1999/02/22-rdf-syntax-ns#type",
        "http://www.w3.org/2000/01/rdf-schema#label",
        "http://www.w3.org/2000/01/rdf-schema#domain",
        "http://www.w3.org/2000/01/rdf-schema#range"
    ]
}

def parse_curriculum_triple(self, triple):
    """Parse a curriculum-specific triple for verbalization"""
    subject, predicate, obj = triple
    
    # Get labels for each part of the triple
    if isinstance(subject, URIRef) and "http://example.org/" in str(subject):
        subject_label = self.get_curriculum_label(subject) or self.extract_label_from_uri(str(subject))
    else:
        subject_label = str(subject)
        
    if isinstance(predicate, URIRef):
        predicate_label = self.get_curriculum_label(predicate) or self.extract_label_from_uri(str(predicate))
        # Convert "has_" prefix to more natural language
        predicate_label = predicate_label.replace("has_", "")
    else:
        predicate_label = str(predicate)
        
    if isinstance(obj, URIRef) and "http://example.org/" in str(obj):
        object_label = self.get_curriculum_label(obj) or self.extract_label_from_uri(str(obj))
    else:
        object_label = str(obj)
    
    # Create a natural language sentence
    return f"{subject_label}'s {predicate_label} is {object_label}"
\end{lstlisting}

\subsubsection{Legal KG Customizations}

For the Legal Knowledge Graph, we implemented specialized URI handling and property exclusions to address the unique challenges of legal document structures:

\begin{lstlisting}[breaklines=true,language=python, caption=Legal KG Customizations, label=code:legal-customizations]
# Legal-specific configuration
legal_config = {
    "prefixes": {
        "lex2kg-o": "https://example.org/lex2kg/ontology/",
        "rdfs": "http://www.w3.org/2000/01/rdf-schema#",
        "xsd": "http://www.w3.org/2001/XMLSchema#"
    },
    "entity_classes": [
        "https://example.org/lex2kg/ontology/Peraturan",
        "https://example.org/lex2kg/ontology/Bab",
        "https://example.org/lex2kg/ontology/Pasal",
        "https://example.org/lex2kg/ontology/Ayat"
    ],
    "primary_entity_class": "https://example.org/lex2kg/ontology/Peraturan",
    "excluded_properties": [
        # Universal properties (same value everywhere)
        "https://example.org/lex2kg/ontology/jenisPeraturan",
        "https://example.org/lex2kg/ontology/bahasa",
        # Technical/internal properties
        "https://example.org/lex2kg/ontology/segmen",
        # Over-granular properties
        "https://example.org/lex2kg/ontology/nomor",
    ]
}

def legal_entity_label(uri):
    """Get label for a legal entity with specialized handling"""
    if not uri or not isinstance(uri, str):
        return ""
        
    # Handle special case for legal documents (UU/year/number)
    if "/uu/" in uri.lower():
        parts = uri.split("/")
        if len(parts) >= 3:
            year_number = "/".join(parts[-2:])
            return f"UU {year_number}"
    
    # Extract last component and clean up
    last_part = uri.split("/")[-1]
    
    # Handle numeric IDs with specific prefixes
    if re.match(r'^[a-z]+\d+$', last_part):
        prefix = re.match(r'^([a-z]+)\d+$', last_part).group(1)
        number = re.match(r'^[a-z]+(\d+)$', last_part).group(1)
        
        if prefix == "bab":
            return f"Bab {number}"
        elif prefix == "pasal":
            return f"Pasal {number}"
        elif prefix == "ayat":
            return f"Ayat {number}"
    
    # Standard label extraction
    label = re.sub(r'([a-z])([A-Z])', r'\1 \2', last_part)
    label = label.replace("_", " ").strip()
    return label.capitalize()
\end{lstlisting}

\subsubsection{GESIS KG Customizations}

For the GESIS Knowledge Graph, we implemented specialized components to handle scholarly data structures and Schema.org-based ontology:

\begin{lstlisting}[breaklines=true,language=python, caption=GESIS KG Customizations, label=code:gesis-customizations]
# GESIS-specific configuration
gesis_config = {
    "prefixes": {
        "schema": "http://schema.org/",
        "ddi": "https://www.ddialliance.org/Specification/DDI-Lifecycle/",
        "gesiskg": "https://www.gesis.org/kg/"
    },
    "entity_classes": [
        "http://schema.org/ScholarlyArticle",
        "http://schema.org/Dataset",
        "http://schema.org/Person",
        "http://schema.org/Organization",
        "http://schema.org/Place",
        "http://schema.org/DefinedTerm"
    ],
    "primary_entity_class": "http://schema.org/ScholarlyArticle",
    "excluded_properties": [
        "http://schema.org/DuplicateMetadata",
        "http://schema.org/validUntil",
        "http://schema.org/contentUrl"
    ]
}

def parse_gesis_triple(self, triple):
    """Parse a GESIS-specific triple for verbalization with Schema.org properties"""
    subject, predicate, obj = triple
    
    # Get labels with GESIS-specific handling
    subject_label = self.get_gesis_label(subject)
    
    # Handle Schema.org properties specially
    if "schema.org" in str(predicate):
        # Convert camelCase to spaces
        predicate_name = str(predicate).split("/")[-1]
        predicate_label = re.sub(r'([a-z])([A-Z])', r'\1 \2', predicate_name).lower()
    else:
        predicate_label = self.get_gesis_label(predicate)
    
    object_label = self.get_gesis_label(obj)
    
    # Create a natural language sentence based on property type
    if predicate_label in ["author", "creator", "contributor"]:
        return f"{object_label} is the {predicate_label} of {subject_label}"
    elif predicate_label in ["date published", "publication date"]:
        return f"{subject_label} was published on {object_label}"
    else:
        return f"The {predicate_label} of {subject_label} is {object_label}"
\end{lstlisting}

\subsection{Post-Processing Pipeline}

The post-processing pipeline is essential for standardizing the format of SPARQL queries in the generated datasets. This standardization improves training efficiency by providing consistent formatting patterns that align with the natural generation tendencies of language models.

\begin{figure}
    \centering
    \includegraphics[width=0.8\linewidth]{assets/pics/hitorinbo.jpg}
    \caption{SPARQL Query Standardization and Dataset Preparation}
    \label{fig:enter-label}
\end{figure}

\subsubsection{SPARQL Query Formatting}

We implemented a comprehensive formatting system for SPARQL queries:

\begin{lstlisting}[breaklines=true,language=python, caption=SPARQL Query Formatting, label=code:sparql-formatting]
def process_json(json_data):
    """Process the JSON data and standardize SPARQL query format"""
    for item in json_data:
        if 'sparql' in item:
            # Convert keywords to uppercase
            item['sparql'] = upper_case_sparql(item['sparql'])
            
            # Replace URIs with prefixed notation where appropriate
            item['sparql'] = replace_uri_with_prefix(item['sparql'])
            
            # Format query structure with proper indentation
            item['sparql'] = format_sparql_query(item['sparql'])
            
            # Remove spaces before ? and . characters
            item['sparql'] = remove_spaces_before_chars(item['sparql'])
    
    return json_data

def upper_case_sparql(sparql_query):
    """Convert SPARQL keywords to uppercase"""
    keywords = [
        "SELECT", "WHERE", "FILTER", "BIND", "UNION", "ORDER BY", "COUNT", "AS",
        "GROUP BY", "LIMIT", "OFFSET", "DISTINCT", "ASK"
    ]
    
    for keyword in keywords:
        sparql_query = re.sub(r'\b' + keyword + r'\b', keyword.upper(), sparql_query, flags=re.IGNORECASE)
    
    return sparql_query

def replace_uri_with_prefix(sparql_query):
    """Replace URIs in the SPARQL query with prefixed notation"""
    # Replace university KG URIs
    sparql_query = re.sub(r'<http://example\.org/([^>]+)>', r'ns1:\1', sparql_query)
    
    # Replace GESIS KG URIs
    sparql_query = re.sub(r'<http://schema\.org/([^>]+)>', r'schema:\1', sparql_query)
    
    # Replace Legal KG URIs - be careful with slashes in identifiers
    sparql_query = re.sub(r'<https://example\.org/lex2kg/ontology/([^/>]+)>', r'lex2kg-o:\1', sparql_query)
    
    return sparql_query

def format_sparql_query(query):
    """Format a SPARQL query with proper indentation"""
    # Clean up the query
    query = query.strip()
    
    # Format basic query structure with WHERE clause
    if "WHERE" in query and "{" in query and "}" in query:
        before_where, after_where = query.split("WHERE", 1)
        before_where = before_where.strip()
        after_where = after_where.strip()
        
        # Handle the WHERE clause
        opening_brace_index = after_where.find("{")
        closing_brace_index = after_where.rfind("}")
        
        if opening_brace_index != -1 and closing_brace_index != -1:
            # Get content inside braces
            content = after_where[opening_brace_index+1:closing_brace_index].strip()
            
            # Format the content with proper indentation
            indented_content = format_where_content(content)
            
            # Get any clauses after the closing brace
            after_clause = after_where[closing_brace_index+1:].strip()
            formatted_after = format_after_clauses(after_clause)
            
            # Build the formatted query
            formatted_query = f"{before_where}\nWHERE {{\n{indented_content}\n}}"
            
            # Add any remaining clauses
            if formatted_after:
                formatted_query += formatted_after
            
            return formatted_query
    
    # Special case for ASK queries
    if query.startswith("ASK") and "WHERE" in query:
        return format_ask_query(query)
    
    # If we can't format it well, return as is
    return query

def format_where_content(content):
    """Format the content inside a WHERE clause with proper indentation"""
    # Handle empty content
    if not content:
        return ""
    
    formatted_lines = []
    
    # Handle special clauses like FILTER, BIND, UNION
    content = handle_special_clauses(content)
    
    # Split into lines, respecting special clauses
    lines = re.split(r'\s*\.\s*(?![^{]*})', content)
    
    for line in lines:
        line = line.strip()
        if not line:
            continue
        
        # Add a period if it's missing and not a special clause
        if not (line.endswith(".") or line.endswith("}") or "FILTER" in line or "BIND" in line):
            line += " ."
            
        # Handle semicolons (multiple predicates with same subject)
        if ";" in line:
            semicolon_parts = handle_semicolons(line)
            formatted_lines.extend(semicolon_parts)
        else:
            # Regular line with 2-space indentation
            formatted_lines.append("  " + line)
    
    return "\n".join(formatted_lines)

def remove_spaces_before_chars(sparql_query):
    """Remove spaces before ? and . in SPARQL queries"""
    # Remove spaces before ? that has one space before it
    modified_query = re.sub(r'(?<! ) \?', '?', sparql_query)
    # Remove spaces before .
    modified_query = re.sub(r'(?<! ) \.', '.', modified_query)
    
    return modified_query
\end{lstlisting}

\subsubsection{Dataset Splitting and Export}

The final step in the post-processing pipeline is splitting the dataset into training and testing sets and exporting them in JSON format:

\begin{lstlisting}[breaklines=true,language=python, caption=Dataset Splitting and Export, label=code:dataset-export]
def prepare_final_dataset(json_data, domain_name, train_ratio=0.67):
    """
    Prepare the final dataset by splitting into train/test sets
    
    Args:
        json_data (list): Processed dataset entries
        domain_name (str): Domain name for file naming
        train_ratio (float): Ratio of data to use for training
        
    Returns:
        tuple: (train_data, test_data)
    """
    # Shuffle data for random split
    random.shuffle(json_data)
    
    # Calculate split point
    split_index = int(len(json_data) * train_ratio)
    
    # Split into train and test sets
    train_data = json_data[:split_index]
    test_data = json_data[split_index:]
    
    # Export train set
    train_file = f"{domain_name}_train.json"
    with open(train_file, 'w') as f:
        json.dump(train_data, f, indent=2)
    
    # Export test set
    test_file = f"{domain_name}_test.json"
    with open(test_file, 'w') as f:
        json.dump(test_data, f, indent=2)
    
    print(f"Dataset split and exported:")
    print(f"  - Training data ({len(train_data)} examples): {train_file}")
    print(f"  - Testing data ({len(test_data)} examples): {test_file}")
    
    return train_data, test_data
\end{lstlisting}

\subsection{End-to-End Dataset Generation Workflow}

The complete dataset generation workflow integrates all components in a modular pipeline:

\begin{lstlisting}[breaklines=true,language=python, caption=End-to-End Dataset Generation Workflow, label=code:end-to-end-workflow]
def generate_dataset(domain_config, output_prefix, size=200):
    """
    Generate a complete dataset for a specific knowledge graph domain
    
    Args:
        domain_config (dict): Domain-specific configuration
        output_prefix (str): Prefix for output files
        size (int): Target dataset size
        
    Returns:
        tuple: (dataset, train_data, test_data)
    """
    # 1. Extract schema from knowledge graph
    schema_extractor = KGSchemaExtractor(domain_config)
    if domain_config.get("sparql_endpoint"):
        schema = schema_extractor.extract_from_endpoint(domain_config["sparql_endpoint"])
    else:
        schema = schema_extractor.extract_from_file(domain_config["ttl_file"], format='turtle')
    
    # 2. Initialize vector search for entity and property matching
    vector_search = KnowledgeGraphVectorSearch()
    vector_search.initialize_collections(
        domain_config["name"],
        pd.DataFrame(schema["entityExamples"]),
        pd.DataFrame(schema["schemaInfo"]["properties"])
    )
    
    # 3. Generate template-based dataset
    template_generator = NL2SPARQLGenerator(
        schema, 
        graph=schema_extractor.graph, 
        vector_search=vector_search
    )
    
    template_size = int(size * 0.6)  # 60% from templates
    template_dataset = template_generator.generate_dataset(
        size=template_size,
        complexity_distribution={"basic": 0.4, "intermediate": 0.3, "advanced": 0.3}
    )
    
    # 4. Generate random-walk dataset
    pattern_generator = EnhancedPatternBasedSPARQLGenerator(
        domain_config["ttl_file"],
        prefixes=domain_config["prefixes"],
        vector_search=vector_search
    )
    
    pattern_size = size - template_size  # 40% from patterns
    pattern_dataset = pattern_generator.generate_dataset(size=pattern_size)
    
    # 5. Generate LLM-assisted content for pattern dataset
    pattern_llm_input = prepare_llm_input(pattern_dataset)
    pattern_llm_content = generate_llm_content(pattern_llm_input, f"{output_prefix}_llm_raw.json")
    pattern_dataset = merge_llm_content_with_patterns(pattern_dataset, pattern_llm_content)
    
    # 6. Combine datasets
    combined_dataset = template_dataset + pattern_dataset
    
    # 7. Apply post-processing
    processed_dataset = process_json(combined_dataset)
    
    # 8. Split and export final dataset
    train_data, test_data = prepare_final_dataset(
        processed_dataset, 
        output_prefix,
        train_ratio=0.67  # 2/3 for training, 1/3 for testing
    )
    
    return processed_dataset, train_data, test_data
\end{lstlisting}

This comprehensive implementation of the dataset generation system enables the creation of high-quality NL2SPARQL datasets for different knowledge graph domains, supporting both template-based and random-walk approaches with extensive customization options and post-processing capabilities.

\section{Model Finetuning Implementation}

Our model finetuning implementation builds upon the dataset generation system to create specialized models for entity property extraction and SPARQL generation. This section details the technical implementation of our finetuning approach across multiple large language models, the prompt design methodology, and the comprehensive evaluation processes employed to validate model performance.

\subsection{Finetuning Infrastructure and Model Selection}

The foundation of our finetuning approach is a modular, efficient infrastructure designed to enable adaptation of large language models with minimal computational requirements. We implement parameter-efficient fine-tuning techniques that allow us to adapt models containing billions of parameters using standard research hardware.

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{assets/pics/model_finetuning_4.jpg}
    \caption{Model Finetuning Infrastructure with Parameter-Efficient Techniques}
    \label{fig:finetuning-infrastructure}
\end{figure}

\subsubsection{Model Selection Strategy}

Our finetuning implementation supports multiple state-of-the-art large language models to enable comprehensive performance comparison across different architecture families. We selected four diverse, instruction-tuned foundation models for adaptation:

\begin{itemize}
    \item \texttt{Qwen/Qwen2.5-Coder-7B-Instruct}: Specialized for code generation tasks with enhanced structured output capabilities
    \item \texttt{Qwen/Qwen2.5-7B-Instruct}: General-purpose instruction-following model with balanced reasoning capabilities
    \item \texttt{meta-llama/Llama-3.1-8B-Instruct}: Latest model from Meta's Llama family with improved instruction following
    \item \texttt{mistralai/Mistral-Nemo-Instruct-2407}: Specialized model based on Mistral architecture with enhanced reasoning capabilities
\end{itemize}

This diverse model selection enables us to evaluate how different architecture families respond to our finetuning approach, with parameter counts ranging from 7 billion to 12 billion parameters.

\subsubsection{Technical Foundation and Libraries}

Our implementation is built on the Unsloth library, which provides optimized fine-tuning capabilities for large language models. The complete technical implementation is provided in \hyperref[code:finetuning-infrastructure]{Appendix~\ref{sec:implementation-code}}.

\subsubsection{Memory Optimization Techniques}

A key challenge in fine-tuning large language models is managing memory constraints. Our implementation incorporates several memory optimization techniques, with complete implementation details provided in \hyperref[code:memory-optimization]{Appendix~\ref{sec:implementation-code}}.

\begin{figure}
    \centering
    \includegraphics[width=0.85\linewidth]{assets/pics/memory_optimization_4.jpg}
    \caption{Memory Optimization Techniques and Their Impact on GPU Memory Usage}
\end{figure}

The memory optimization techniques include:

\begin{enumerate}
    \item \textbf{4-bit Quantization}: Models are loaded with 4-bit precision, reducing memory requirements by up to 8x compared to full precision.
    
    \item \textbf{Gradient Checkpointing}: Implemented via the \texttt{use\_gradient\_checkpointing} parameter, this technique trades computation for memory by recomputing intermediate activations during backpropagation rather than storing them.
    
    \item \textbf{Memory-Efficient Optimizers}: We use 8-bit optimizers to reduce the memory footprint of optimizer states.
    
    \item \textbf{Aggressive Memory Clearing}: Between training phases, we employ aggressive memory clearing with explicit synchronization to ensure maximum memory availability.
\end{enumerate}

These optimizations enable us to fine-tune 7-8 billion parameter models on a single GPU with 16GB of memory, making our approach accessible to researchers with limited computational resources.

\subsection{Entity and Property Extraction Model}

The Entity and Property Extraction model serves as the foundation for our question-answering pipeline, transforming natural language questions into structured entity and property identifications that can be used for subsequent SPARQL generation.


\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{assets/pics/ta-gsx.jpg}
    \caption{Entity and Property Extraction Model Architecture}
    \label{fig:entity-extraction-model}
\end{figure}

\subsubsection{Task Definition and Prompt Design}

The entity and property extraction task is framed as a structured generation problem. We designed a comprehensive system prompt that specifies the extraction task parameters and expected output format:

\begin{tcolorbox}[title=Entity and Property Extraction System Prompt, colback=blue!5!white, colframe=blue!75!black, boxrule=0.5mm, arc=2mm, fontupper=\ttfamily\small, breakable]
You are an expert entity and property extractor for knowledge graph querying. Your task is to analyze a natural language question and identify the relevant entities and properties needed to create a SPARQL query for Wikidata. \\

Guidelines: \\
1. For each question, extract ALL entities mentioned in the question \\
2. For each question, extract ALL relevant properties needed to answer the question \\
3. Format your response as a structured JSON object with 'entities' and 'properties' keys \\
4. Each key should contain an array of strings with the entity or property names \\
5. Focus ONLY on extracting, not on generating SPARQL queries \\

Your output should look like: \\
```json \\
\{ \\
  "entities": ["entity1", "entity2", ...], \\
  "properties": ["property1", "property2",  \\...] \\
\} \\
```
\end{tcolorbox}

Example user prompts follow this pattern:

\begin{tcolorbox}[title=Entity and Property Extraction User Prompt, colback=green!5!white, colframe=green!75!black, boxrule=0.5mm, arc=2mm, fontupper=\ttfamily\small, breakable]
Question: What is the population of France? \\

Extract all entities and properties from this question that would be needed to generate a SPARQL query for Wikidata.
\end{tcolorbox}

The expected assistant output is a structured JSON response:

\begin{tcolorbox}[title=Entity and Property Extraction Assistant Response, colback=orange!5!white, colframe=orange!75!black, boxrule=0.5mm, arc=2mm, fontupper=\ttfamily\small, breakable]
```json \\
\{ \\
  "entities": ["France"], \\
  "properties": ["population"] \\
\} \\
```
\end{tcolorbox}

\subsubsection{Low-Rank Adaptation Implementation}

For the entity extraction task, we implemented a specialized QLoRA configuration optimized for structured extraction tasks. The complete implementation is provided in \hyperref[code:entity-extraction-lora]{Appendix~\ref{sec:implementation-code}}.

This configuration uses a moderate rank (r=16) which we determined empirically to provide sufficient capacity for the entity extraction task while maintaining computational efficiency. The approach achieves approximately 99\% parameter efficiency compared to full model fine-tuning, using only about 40-60 million trainable parameters depending on the base model size.

\subsection{SPARQL Generation Model}

The SPARQL Generation model represents the core reasoning component of our system, transforming identified entities and properties into executable SPARQL queries. This task requires more sophisticated adaptation to capture complex syntax patterns and domain-specific knowledge.

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{assets/pics/ta-gsx-2.jpg}
    \caption{SPARQL Generation Model with Enhanced LoRA Configuration}
    \label{fig:sparql-generation-model}
\end{figure}

\subsubsection{Task Definition and Prompt Design}

The SPARQL generation task is structured as a reasoning-enhanced generation problem. We designed a comprehensive system prompt that specifies both the reasoning process and query generation requirements:

\begin{tcolorbox}[title=SPARQL Generation System Prompt, colback=blue!5!white, colframe=blue!75!black, boxrule=0.5mm, arc=2mm, fontupper=\ttfamily\small, breakable]
You are a SPARQL generator expert for Wikidata knowledge graph. Your task is to convert the following natural language question to a SPARQL query for Wikidata using the provided entity and property resolutions. \\

Guidelines: \\
1. First identify which entities from the list match the question's intent \\
2. Identify which entities are relevant to the question and select EXACTLY ONE entity ID for each distinct concept in the question \\
3. When multiple entities have similar labels, choose the one whose description best matches the question's context \\
4. From the properties list, choose which properties are needed to answer the question \\
5. Select only the minimum necessary properties required to answer the question correctly \\
6. Use ALL identified entities and necessary properties in your SPARQL query \\
7. Use PREFIX NOTATION ONLY (e.g., wd:Q123, wdt:P123), NOT full URIs \\
8. Optimize your query by using appropriate SPARQL features (DISTINCT, FILTER, ORDER BY, LIMIT) when needed \\
9. Return entity IDs directly without using label services \\
10. Return ONLY the raw SPARQL query with no explanations or comments in this format: \\
   ```sparql \\
   <your\_sparql\_query\_here> \\
   ``` \\

IMPORTANT: Before generating the SPARQL query, provide your step-by-step reasoning about how to construct the query. \\
\end{tcolorbox}

Example user prompts include the question and candidate entities and properties:

\begin{tcolorbox}[title=SPARQL Generation User Prompt, colback=green!5!white, colframe=green!75!black, boxrule=0.5mm, arc=2mm, fontupper=\ttfamily\small, breakable]
Question: What is the population of France? \\
Entities: \\
- id: Q142, label: France, description: republic in Western Europe \\
Properties: \\
- id: P1082, label: population, description: number of people inhabiting the place \\
- id: P31, label: instance of, description: that class of which this subject is a particular example and member \\
- id: P17, label: country, description: sovereign state of this item \\
SPARQL: \\
\end{tcolorbox}

The expected assistant output includes both reasoning steps and the SPARQL query:

\begin{tcolorbox}[title=SPARQL Generation Assistant Response, colback=orange!5!white, colframe=orange!75!black, boxrule=0.5mm, arc=2mm, fontupper=\ttfamily\small, breakable]
Let me analyze this step by step: \\

1. The question asks for the population of France. \\
2. From the entities list, "France" (Q142) is clearly the relevant entity. \\
3. From the properties list, "population" (P1082) is the property needed to get the population value. \\
4. I need to construct a query that retrieves the population value of France. \\
5. The structure will be a simple triple pattern: wd:Q142 wdt:P1082 ?population. \\

```sparql \\
SELECT ?population WHERE \{ \\
  wd:Q142 wdt:P1082 ?population. \\
\} \\
``` \\
\end{tcolorbox}

\subsubsection{Enhanced LoRA Configuration for SPARQL Generation}

For the SPARQL generation task, we implemented a more extensive QLoRA configuration with higher rank and additional adaptations. The complete implementation is provided in \hyperref[code:sparql-generation-lora]{Appendix~\ref{sec:implementation-code}}.

This enhanced configuration uses a variable rank (r=32 or r=64 depending on the knowledge graph) to provide appropriate capacity for learning complex SPARQL syntax patterns. Specifically, a lower rank (r=32) is used for Legal and GESIS knowledge graphs which have longer inputs, while a higher rank (r=64) is used for Wikidata and Curriculum knowledge graphs. We also implemented rank-stabilized QLoRA (RSQLoRA) to prevent training instabilities that can occur with higher-rank adapters. The configuration results in approximately 160-230 million trainable parameters, still achieving over 97\% parameter efficiency compared to full model fine-tuning.

\subsection{Dataset Formatting and Processing}

A critical aspect of our finetuning implementation is the careful formatting of training examples to maximize learning efficiency. We implemented specialized formatting functions for both entity extraction and SPARQL generation tasks.

\subsubsection{Entity and Property Extraction Data Formatting}

For entity and property extraction, we format training examples to include the system instructions, question input, and expected JSON output:

\begin{lstlisting}[breaklines=true,language=python, caption=Entity Extraction Data Formatting, label=code:entity-extraction-formatting]
def format_instruction(row):
    # Format entities and properties as JSON arrays
    entities_json = json.dumps(row['entities'])
    properties_json = json.dumps(row['properties'])
    
    user_prompt = f"""Question: {row['question']}

Extract all entities and properties from this question that would be needed to generate a SPARQL query for Wikidata."""

    asst_prompt = f"""```json
{{
  "entities": {entities_json},
  "properties": {properties_json}
}}
```"""

    if has_chat_template:
        prompt = tokenizer.apply_chat_template(
            [
                {"role": "system", "content": system_prompt},
                {"role": "user", "content": user_prompt},
                {"role": "assistant", "content": asst_prompt}
            ],
            tokenize=False,
            add_generation_prompt=False,
            continue_final_message=False
        )
    else:
        prompt = system_prompt + '\n' + user_prompt + '\n' + asst_prompt
    
    return prompt
\end{lstlisting}

\subsubsection{SPARQL Generation Data Formatting}

For SPARQL generation, we format training examples to include the system instructions, question input with entity and property candidates, and the expected output with reasoning steps and SPARQL query:

\begin{lstlisting}[breaklines=true,language=python, caption=SPARQL Generation Data Formatting, label=code:sparql-generation-formatting]
def format_instruction(row):
    entities_matches_formatted = format_entity_matches(row['entities_matches'])
    properties_matches_formatted = format_property_matches(row['properties_matches'])

    user_prompt = f"""Question: {row['question']}

Entities:
{entities_matches_formatted}

Properties:
{properties_matches_formatted}

SPARQL:"""
    
    # Format thoughts to be more readable in the assistant's response
    thoughts_formatted = "Let me analyze this step by step:\n\n"
    for i, thought in enumerate(row['thoughts'], 1):
        thoughts_formatted += f"{thought}\n"
    
    asst_prompt = f"""{thoughts_formatted}

```sparql
{row['sparql']}
```"""

    if has_chat_template:
        prompt = tokenizer.apply_chat_template(
            [
                {"role": "system", "content": system_prompt},
                {"role": "user", "content": user_prompt},
                {"role": "assistant", "content": asst_prompt}
            ],
            tokenize=False,
            add_generation_prompt=False,
            continue_final_message=False
        )
    else:
        prompt = system_prompt + '\n' + user_prompt + '\n' + asst_prompt
    
    return prompt
\end{lstlisting}

Our implementation carefully handles model-specific chat templates, ensuring that examples are properly formatted for each model's expected input format. For models without native chat templates, we apply appropriate formatting to maintain consistency.

\subsection{Model Export and Deployment Strategy}

A critical component of our implementation is the ability to export fine-tuned models in formats compatible with deployment environments, particularly for integration with the LangGraph-based multi-agent system. While we initially save models in the standard Hugging Face format, we implemented additional export functionality to enhance deployment flexibility.

\subsubsection{GGUF Conversion and Hugging Face Integration}

For efficient deployment in production environments, particularly with the Ollama inference server used by our LangGraph agents, we implemented GGUF (GPT-Generated Unified Format) conversion capabilities:

\begin{lstlisting}[breaklines=true,language=python, caption=GGUF Conversion and Hugging Face Upload, label=code:gguf-conversion]
def export_model_to_gguf(model, tokenizer, model_name, repo_suffix, hf_token):
    """
    Convert the fine-tuned model to GGUF format and upload to Hugging Face
    
    Args:
        model: Fine-tuned model
        tokenizer: Associated tokenizer
        model_name: Base model name
        repo_suffix: Suffix to append to repository name
        hf_token: Hugging Face API token
    """
    # Create repository name based on model name and task
    save_repo_name = f"gansixeneh/{model_name.split('/')[-1]}"
    repo_name = f"{save_repo_name}-{repo_suffix}"
    
    # Create or reuse repository on Hugging Face
    create_repo(repo_name, token=hf_token, exist_ok=True)
    
    # Convert and push model to Hugging Face in GGUF format
    # q4_k_m provides optimal balance of size and quality
    model.push_to_hub_gguf(
        repo_name, 
        tokenizer, 
        quantization_method="q4_k_m",  # 4-bit quantization with medium precision
        token=hf_token
    )
    
    # Clean up temporary files
    if os.path.exists('/kaggle/working/gansixeneh'):
        shutil.rmtree('/kaggle/working/gansixeneh')
        
    print(f"Model successfully exported to {repo_name} in GGUF format")
\end{lstlisting}

This functionality is integrated into our fine-tuning workflows through a conditional export process:

\begin{lstlisting}[breaklines=true,language=python, caption=Conditional GGUF Export in Fine-tuning Workflow, label=code:conditional-gguf]
# In both finetune_sparql and finetune_entity_prop functions:
# After saving the model in standard format
model.save_pretrained(output_dir)
tokenizer.save_pretrained(output_dir)

# Conditional GGUF conversion and upload
if save_to_gguf:
    save_repo_name = f"gansixeneh/{model_name.split('/')[-1]}"
    
    # Create different repository names based on task
    if task_type == "sparql":
        repo_name = f"{save_repo_name}-sparql-{dataset_type}"
    else:
        repo_name = f"{save_repo_name}-extract-entity-{dataset_type}"
        
    # Create repository and push model in GGUF format
    create_repo(repo_name, token=hf_token, exist_ok=True)
    model.push_to_hub_gguf(
        repo_name, 
        tokenizer, 
        quantization_method="q4_k_m", 
        token=hf_token
    )
\end{lstlisting}

\subsubsection{Integration with Ollama for LangGraph Agents}

The GGUF-formatted models hosted on Hugging Face enable seamless integration with Ollama, the inference server used by our LangGraph multi-agent system. This integration is implemented through Ollama's model import capabilities:

\begin{lstlisting}[breaklines=true,language=python, caption=Ollama Integration for LangGraph, label=code:ollama-integration]
def setup_ollama_model(model_repo, model_name):
    """
    Import a GGUF model from Hugging Face to Ollama for LangGraph agent use
    
    Args:
        model_repo: Hugging Face repository name
        model_name: Local name for Ollama
    """
    # Ollama modelfile definition
    modelfile = f"""
    FROM {model_repo}
    
    # System message to define agent behavior
    SYSTEM """You are a specialized agent in the FROG system."""
    
    # Set appropriate parameters for agent inference
    PARAMETER temperature 0.1
    PARAMETER num_ctx 4096
    """
    
    # Write modelfile
    with open("Modelfile", "w") as f:
        f.write(modelfile)
    
    # Create Ollama model
    os.system(f"ollama create {model_name} -f Modelfile")
    
    return model_name
\end{lstlisting}

This deployment strategy enables our LangGraph agents to leverage our fine-tuned models efficiently, with the benefits of:

\begin{itemize}
    \item \textbf{Reduced Size}: The q4\_k\_m quantization reduces model size by approximately 8x compared to full precision
    \item \textbf{Inference Optimization}: GGUF format enables optimized inference on CPU and consumer GPUs
    \item \textbf{Deployment Flexibility}: Models can be easily distributed and deployed across different environments
    \item \textbf{Version Control}: Hugging Face integration provides model versioning and metadata management
\end{itemize}

By implementing this export and deployment pipeline, we enable seamless transition from model fine-tuning to production deployment in our multi-agent LangGraph system, while maintaining computational efficiency through optimized formats.

\subsection{Training Process and Hyperparameter Selection}

Our training process involves careful hyperparameter selection and monitoring to ensure optimal model performance. We implemented comprehensive logging and validation to track model progress throughout training.

\begin{figure}
    \centering
    \includegraphics[width=0.7\linewidth]{assets//pics/training_flow_4.jpg}
    \caption{Training Process with Learning Curves and Validation Monitoring}
    \label{fig:training-process}
\end{figure}

\subsubsection{Hyperparameter Selection Strategy}

We employed a methodical approach to hyperparameter selection, balancing performance and computational efficiency:

\begin{itemize}
    \item \textbf{Entity Extraction Model}: Moderate rank (r=16), no dropout, no bias training, standard QLoRA, linear learning rate schedule, 5 epochs
    \item \textbf{SPARQL Generation Model}: Variable rank (r=32 for Legal/GESIS KGs, r=64 for Wikidata/Curriculum KGs), moderate dropout (0.1), bias training, rank-stabilized QLoRA, polynomial learning rate schedule, 10 epochs
\end{itemize}

These hyperparameter configurations were determined through empirical testing on validation sets, with a focus on minimizing validation loss while preventing overfitting.

\subsubsection{Training Monitoring and Checkpointing}

Our implementation includes comprehensive training monitoring and checkpointing to ensure optimal model selection:

\begin{lstlisting}[breaklines=true,language=python, caption=Training Monitoring Implementation, label=code:training-monitoring]
# Checkpoint management in TrainingArguments
save_total_limit=5,            # Keep last 5 checkpoints
load_best_model_at_end=True,   # Load best model after training
metric_for_best_model="eval_loss",  # Use validation loss for selection
greater_is_better=False,       # Lower loss is better
\end{lstlisting}

This approach ensures that only the best-performing model on validation data is retained for deployment, mitigating the risk of overfitting.

\subsection{Model Evaluation Methodology}

Our finetuning implementation includes a comprehensive evaluation methodology to assess model performance across different tasks and knowledge domains. We employ an end-to-end assessment approach to provide a holistic view of model capabilities.

\begin{lstlisting}[breaklines=true,language=python, caption=End-to-End Evaluation Implementation, label=code:end-to-end-evaluation]
def evaluate_batch_results(results_df, ground_truth_queries, questions, complexities):
    """
    Evaluate the batch results from the FROG pipeline with complexity-based grouping
    """
    evaluation_results = []
    
    for idx, row in results_df.iterrows():
        question = row['question']
        generated_query = row.get('generated_sparql', '')
        sparql_results = row.get('sparql_results', [])
        verbalization_results = row.get("verbalization_results", [])
        verbalization_score = row.get('verbalization_scores', 0.0)
        used_verbalization = row.get('used_verbalization', False)
        ground_truth_query = ground_truth_queries[idx]
        complexity = complexities[idx]
        
        # Execute ground truth query
        try:
            ground_truth_df = pd.DataFrame(ground_truth_results)
        except Exception as e:
            ground_truth_df = pd.DataFrame()
            
        # Determine which results to use
        if used_verbalization and verbalization_results:
            # Use verbalization results
            pred_df = pd.DataFrame(verbalization_results)
        elif sparql_results:
            # Use SPARQL results
            pred_df = pd.DataFrame(sparql_results)
        else:
            pred_df = pd.DataFrame()
            
        # Calculate metrics
        if not pred_df.empty and not ground_truth_df.empty:
            metrics = compare_two_dataframes(ground_truth_df, pred_df)
            # Record evaluation results
            result_entry = {
                "question_id": idx,
                "question_text": question,
                "complexity": complexity,
                "ground_truth_query": ground_truth_query,
                "generated_query": generated_query,
                "entities": row.get('entities', []),
                "properties": row.get('properties', []),
                "entity_uris": row.get('entity_uris', []),
                "verbalization_score": verbalization_score,
                "used_verbalization": used_verbalization,
                "sparql_generation_attempts": row.get('sparql_generation_attempts', 0),
                "approach_used": "verbalization" if used_verbalization else "sparql_generation",
                "metrics": metrics
            }
            evaluation_results.append(result_entry)
        
    return evaluation_results
\end{lstlisting}

Our evaluation approach includes stratified assessment by complexity level (for domain-specific knowledge graphs) and comprehensive comparison between base and fine-tuned models. This structured evaluation methodology enables detailed understanding of model performance across different scenarios and knowledge domains.

\subsubsection{Reproducibility Configuration}
To ensure consistent and reproducible evaluation results across multiple runs, we implemented comprehensive deterministic behavior configuration in our evaluation pipeline:

\begin{lstlisting}[breaklines=true,language=python, caption=Deterministic Configuration for Reproducibility, label=code:deterministic-config]
# Set up deterministic behavior for reproducibility
import os
os.environ['CUBLAS_WORKSPACE_CONFIG'] = ':4096:8'

import torch
import random
import numpy as np

random.seed(42)
np.random.seed(42)
torch.manual_seed(42)
torch.cuda.manual_seed_all(42)
torch.backends.cudnn.deterministic = True
torch.backends.cudnn.benchmark = False
torch.use_deterministic_algorithms(True)
\end{lstlisting}

Additionally, all models were explicitly set to evaluation mode before inference to ensure deterministic forward passes:

\begin{lstlisting}[breaklines=true,language=python, caption=Model Evaluation Mode Setting, label=code:model-eval-mode]
self.model = SentenceTransformer(model_name, **model_kwargs)
self.model.eval()
\end{lstlisting}

This configuration was essential for meaningful comparison between model variants and evaluation runs, ensuring that any observed performance differences were attributable to actual model capabilities rather than random initialization or stochastic behavior during inference.

\subsection{Implementation Challenges and Solutions}

Throughout the implementation process, we encountered and addressed several technical challenges:

\begin{enumerate}
    \item \textbf{Memory Limitations}: We addressed GPU memory constraints through aggressive optimization techniques including 4-bit quantization, gradient checkpointing, and memory-efficient optimizers.
    
    \begin{lstlisting}[breaklines=true,language=python, caption=Memory Optimization Techniques, label=code:memory-optimization]
    # Memory-optimized model loading
    model, tokenizer = FastLanguageModel.from_pretrained(
        model_name=model_name,
        max_seq_length=2048,
        load_in_4bit=True,  # 4-bit quantization (8x memory reduction)
        dtype=None          # Auto-detect optimal precision
    )
    
    # Memory-efficient QLoRA configuration
    model = FastLanguageModel.get_peft_model(
        model,
        r=64,  # Rank parameter
        use_gradient_checkpointing="unsloth",  # Trade computation for memory
        # Only these modules get QLoRA adapters (saves memory)
        target_modules=["q_proj", "k_proj", "v_proj", "o_proj", 
                        "gate_proj", "up_proj", "down_proj"]
    )
    \end{lstlisting}
    
    \item \textbf{Model Compatibility}: Each model family required specific adaptations to ensure compatibility with our training approach. We implemented model-specific chat template handling and tokenization to ensure consistent behavior, especially for \texttt{Qwen/Qwen2.5-7B-Instruct} where it doesn't have its own chat template.
    
    \begin{lstlisting}[breaklines=true,language=python, caption=Model-Specific Compatibility Handling, label=code:model-compatibility]
    # Model-specific chat template handling
    def apply_model_specific_settings(model, tokenizer):
        # Set model-specific chat templates
        if 'qwen' in model.config.name_or_path:
            tokenizer.chat_template = "<|im_start|>system\n{{ messages[0]['content'] }}<|im_end|>\n{% for message in messages[1:] %}<|im_start|>{{ message['role'] }}\n{{ message['content'] }}<|im_end|>\n{% endfor %}"
        elif 'mistral' in model.config.name_or_path:
            # Mistral uses its own template by default
            pass
        elif 'llama' in model.config.name_or_path:
            # Llama-specific handling if needed
            pass
            
        # Verify proper configuration
        has_chat_template = hasattr(tokenizer, "chat_template") and tokenizer.chat_template is not None
        return has_chat_template
    \end{lstlisting}
    
    \item \textbf{Training Stability}: Higher-rank QLoRA configurations occasionally experienced training instabilities, particularly for SPARQL generation. We implemented rank-stabilized QLoRA (RSQLoRA) to address this issue.
    
    \begin{lstlisting}[breaklines=true,language=python, caption=Training Stability Improvements, label=code:training-stability]
    # Standard QLoRA for entity extraction (more stable task)
    entity_model = FastLanguageModel.get_peft_model(
        base_model,
        r=16,                  # Lower rank for stability
        lora_alpha=16,
        lora_dropout=0,
        use_rslora=False,      # Standard QLoRA is sufficient
        bias="none"            # Fewer parameters to update
    )
    
    # Enhanced stability for SPARQL generation (complex task)
    sparql_model = FastLanguageModel.get_peft_model(
        base_model,
        r=64,                  # Higher rank needed for complexity (r=32 for Legal/GESIS KGs, r=64 for Wikidata/Curriculum KGs)
        lora_alpha=64,
        lora_dropout=0.1,      # Dropout for generalization
        use_rslora=True,       # Rank-Stabilized QLoRA for stability
        bias="all"             # Include bias for syntax learning
    )
    \end{lstlisting}
        
    \item \textbf{Component Initialization Order}: We discovered critical incompatibilities between Unsloth and Weaviate when both were initialized in the same Python process. Specifically, Unsloth's CUDA optimizations conflicted with Weaviate's GPU memory management. This required careful implementation sequencing where Weaviate client initialization had to be performed after all Unsloth fine-tuning processes were completed:

    \begin{lstlisting}[breaklines=true,language=python, caption=Component Initialization Order, label=code:initialization-order]
    # INCORRECT implementation (causes memory conflicts)
    # weaviate_client = initialize_weaviate()
    # model, tokenizer = finetune_with_unsloth()
    
    # CORRECT implementation
    model, tokenizer = finetune_with_unsloth()
    clear_memory()  # Explicitly clear GPU memory
    torch.cuda.empty_cache()
    gc.collect()
    
    # Only initialize Weaviate after Unsloth is done
    weaviate_client = initialize_weaviate()
    \end{lstlisting}

    This ordering constraint required careful restructuring of our pipeline to separate fine-tuning and inference phases completely.

    \item \textbf{Package Dependency Instability}: A significant challenge was managing package dependencies with rapidly evolving libraries. For example, we discovered that langchain-huggingface $\geq$ 0.2.0 introduced breaking changes that were incompatible with our Unsloth implementation:

    \begin{lstlisting}[breaklines=true,language=python, caption=Package Dependency Issue Example, label=code:dependency-issue]
    # This fails with newer versions of langchain-huggingface
    from langchain_huggingface import ChatHuggingFace
    
    # Error: AttributeError: module 'langchain_huggingface' has no attribute 'ChatHuggingFace'
    # in versions >= 0.2.0 where the API was restructured
    \end{lstlisting}
    
    To address this, we had to explicitly pin langchain-huggingface to version 0.1.2 in our installation requirements. Without such version pinning, the same code would fail when dependencies received updates, highlighting the fragility of deep learning pipelines that rely on multiple rapidly evolving libraries.
    
    Additionally, we found that the \texttt{protobuf} package version was critical for compatibility between Weaviate and our model processing pipeline. The version needed to be specifically upgraded with the command \texttt{pip install --upgrade -q protobuf} to prevent serialization errors when moving between the fine-tuning and vector database components of our system.
    
    \item \textbf{Tokenizer Inconsistencies}: We encountered issues where tokenizers initialized differently across model runs, particularly with the Qwen2.5 models. This required explicit chat template verification and setting even when the documentation suggested it should be automatically loaded:

    \begin{lstlisting}[breaklines=true,language=python, caption=Tokenizer Template Verification, label=code:tokenizer-verification]
    # Always verify and explicitly set template regardless of hasattr check
    if 'qwen' in model.config.name_or_path:
        tokenizer.chat_template = "<|im_start|>system\n{{ messages[0]['content'] }}<|im_end|>\n{% for message in messages[1:] %}<|im_start|>{{ message['role'] }}\n{{ message['content'] }}<|im_end|>\n{% endfor %}"
        print("Model's tokenizer has been set.")
    
    # Verify template was properly set
    has_chat_template = hasattr(tokenizer, "chat_template") and tokenizer.chat_template is not None
    if not has_chat_template:
        raise ValueError(f"Chat template not properly set for {model_name}")
    \end{lstlisting}
    
    \item \textbf{Resource Deallocation}: Proper resource deallocation became critical for the stability of our pipeline. We found that standard Python garbage collection was insufficient, and explicit model deletion and CUDA cache clearing were necessary between processing stages:

    \begin{lstlisting}[breaklines=true,language=python, caption=Resource Deallocation Implementation, label=code:resource-deallocation]
    def clean_model_resources():
        """Aggressive resource cleanup between model loads"""
        # Delete model explicitly
        if 'model' in globals():
            del globals()['model']
        
        # Delete tokenizer explicitly 
        if 'tokenizer' in globals():
            del globals()['tokenizer']
        
        # Clear CUDA cache with synchronization
        torch.cuda.empty_cache()
        if torch.cuda.is_available():
            torch.cuda.synchronize()
        
        # Force garbage collection multiple times
        for _ in range(3):
            gc.collect()
    \end{lstlisting}
\end{enumerate}

These implementation challenges highlight the complexity of building production-ready systems that integrate multiple deep learning and vector database components. Our solutions ensured reproducible behavior across different environments while maintaining the performance benefits of our optimized approach.

\section{Multi-Agent System Implementation}
The multi-agent system represents the core architectural component of our enhanced GraphRAG framework. We implemented a sophisticated orchestration of specialized agents using LangGraph, a directed graph-based framework designed for composing language agents. This section details the technical implementation of our multi-agent architecture, including the state graph construction, multi-source knowledge graph infrastructure, specialized agent nodes, and integration with our multi-provider LLM factory.

\subsection{LangGraph Architecture}
The foundation of our multi-agent system is built on LangGraph, which provides a structured approach to defining agent workflows through directed state graphs. The complete implementation is provided in \hyperref[code:state-graph]{Appendix~\ref{sec:implementation-code}}.

The state graph implementation offers several technical advantages over a linear pipeline:

\begin{enumerate}
    \item \textbf{Conditional Routing}: The \texttt{add\_conditional\_edges} method enables dynamic workflow decisions based on the current state. This allows us to implement adaptive processing paths depending on query complexity and agent outputs.
    
    \item \textbf{Shared State}: All agents operate on a shared \texttt{FROGGraphRAGState} object, which maintains cumulative processing context across the workflow. This eliminates the need for complex data passing between components.
    
    \item \textbf{Modular Component Replacement}: Individual nodes can be replaced or modified without affecting the overall graph structure, enabling easy experimentation with different implementations.
\end{enumerate}

\begin{figure}
    \centering
    \includegraphics[width=0.7\linewidth]{assets//pics/langgraph_architecture_4.jpg}
    \caption{LangGraph Multi-Agent Workflow Architecture}
    \label{fig:enter-label}
\end{figure}

\subsubsection{State Management Implementation}
The core of our multi-agent system is the \texttt{FROGGraphRAGState} class, which defines the shared state that flows through the agent graph. The complete implementation is provided in \hyperref[code:state-management]{Appendix~\ref{sec:implementation-code}}.

The state object captures all processing context, including:

\begin{itemize}
    \item Input and processed questions (original and translated)
    \item Extracted entities and properties
    \item Generated SPARQL queries
    \item Query execution results
    \item Processing decisions and approach indicators
    \item Runtime settings like verbalization and Google search preferences
    \item Knowledge source identification
    \item Visualization and debugging resources
\end{itemize}

This comprehensive state design ensures that each agent has access to all previous processing context, enabling more informed decision-making at each step of the workflow.

\subsection{Multi-Source Knowledge Graph Architecture}
Our implementation utilizes a source-aware architecture that enables seamless integration with multiple knowledge graphs. This flexibility allows the system to handle diverse knowledge domains through a unified interface while accommodating domain-specific requirements.

\subsubsection{Knowledge Graph Metadata System}
The foundation of our multi-source architecture is a comprehensive metadata system that defines the configuration for each knowledge graph. The complete configuration structure is provided in \hyperref[code:kg-metadata]{Appendix~\ref{sec:implementation-code}}.

For each knowledge graph, the metadata includes:
\begin{itemize}
    \item \textbf{SPARQL Endpoint}: URL for the knowledge graph's query service
    \item \textbf{Namespace Prefixes}: Domain-specific URI prefixes for entities and properties
    \item \textbf{SPARQL Instructions}: Domain-specific guidelines for query formatting
    \item \textbf{Verbalization Templates}: Pre-defined SPARQL templates for property retrieval
    \item \textbf{URI Formats}: Templates for entity and property URI construction
    \item \textbf{Reference Support}: Indicator of whether the graph supports reference queries
\end{itemize}

\subsubsection{Source-Aware SPARQL Wrapper}
To handle the specific requirements of different knowledge graphs, we implemented a source-aware SPARQL wrapper that adapts query execution based on the knowledge source:

\begin{lstlisting}[breaklines=true,language=python, caption=Source-Aware SPARQL Wrapper Implementation, label=code:source-aware-sparql]
class SourceAwareSPARQLWrapper:
    """SPARQL wrapper that adapts to different knowledge graph sources"""
    def __init__(self, source="wikidata"):
        self.source = source
        self.kg_metadata = get_knowledge_graph_metadata()
        
        # Get endpoint URL for the current source
        self.endpoint_url = self.kg_metadata.get_endpoint_url(source)
        
        # Initialize the SPARQLWrapper with the appropriate endpoint
        self.wrapper = SPARQLWrapper(self.endpoint_url)
        self.wrapper.setReturnFormat(JSON)
        
        # Set appropriate user agent
        user_agent = self.kg_metadata.get_user_agent(source)
        if user_agent:
            self.wrapper.addCustomHttpHeader("User-Agent", user_agent)
    
    def execute_query(self, query, return_format="list"):
        """Execute a SPARQL query against the appropriate endpoint"""
        try:
            # Apply environment variable substitution for local endpoints
            processed_query = self._process_query(query)
            
            # Set and execute the query
            self.wrapper.setQuery(processed_query)
            results = self.wrapper.query().convert()
            
            # Return formatted results based on requested format
            if return_format == "list":
                return self._parse_results_as_list(results), None
            else:
                return self._parse_results_as_dict(results), None
                
        except Exception as e:
            return None, str(e)
\end{lstlisting}

The source-aware wrapper provides several key technical advantages:

\begin{enumerate}
    \item \textbf{Dynamic Endpoint Selection}: Automatically selects the appropriate SPARQL endpoint based on the knowledge source.
    \item \textbf{Environment Variable Substitution}: Handles local endpoints with environment variable configuration.
    \item \textbf{Result Format Normalization}: Standardizes result formats across different knowledge graph implementations.
    \item \textbf{Error Handling}: Provides consistent error handling regardless of the underlying knowledge graph.
\end{enumerate}

\subsubsection{Factory Pattern for Knowledge Graph Services}

To enable seamless switching between knowledge graph sources, we implemented a factory pattern for knowledge graph services. This architecture allows components to request appropriate services without needing to know the specific implementation details:

\begin{lstlisting}[breaklines=true,language=python, caption=Property Retrieval Factory Implementation, label=code:property-retrieval-factory]
class PropertyRetrievalFactory:
    """
    Factory for creating property retrievers for different knowledge graph sources
    """
    def __init__(self):
        self._retrievers = {}
        
    def get_property_retriever(self, source, df_properties=None):
        """
        Get or create a property retriever for the specified source
        
        Args:
            source (str): Knowledge graph source identifier
            df_properties (pd.DataFrame): Optional properties dataframe for Wikidata
            
        Returns:
            object: Property retriever for the specified source
        """
        if source in self._retrievers:
            return self._retrievers[source]
            
        # Create a new retriever based on the source
        if source == "wikidata":
            if df_properties is None:
                raise ValueError("df_properties must be provided for Wikidata")
            retriever = WikidataPropertyRetrieval(df_properties)
        elif source == "curriculum":
            retriever = CurriculumPropertyRetrieval()
        elif source == "legal":
            retriever = LegalPropertyRetrieval()
        elif source == "gesis":
            retriever = GesisPropertyRetrieval()
        else:
            raise ValueError(f"Unsupported source: {source}")
            
        # Cache and return the retriever
        self._retrievers[source] = retriever
        return retriever

# Singleton instance
_property_retrieval_factory = None

def get_property_retrieval_factory():
    """Get the singleton property retrieval factory instance"""
    global _property_retrieval_factory
    if _property_retrieval_factory is None:
        _property_retrieval_factory = PropertyRetrievalFactory()
    return _property_retrieval_factory
\end{lstlisting}

This factory pattern is applied to multiple services in our architecture, including:
\begin{itemize}
    \item \textbf{Property Retrieval}: For finding relevant entities and properties in knowledge graphs
    \item \textbf{Entity Retrieval}: For finding entities matching natural language terms
    \item \textbf{SPARQL Execution}: For executing queries against different endpoints
\end{itemize}

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{assets//pics/kg_extractor_uml.jpg}
    \caption{Multi-Source Knowledge Graph Architecture}
\end{figure}

\subsection{Specialized Agent Nodes}
Our multi-agent system consists of eight specialized agent nodes, each implemented as a separate Python class with a standardized interface. All nodes follow a common pattern where they:
\begin{enumerate}
    \item Accept a \texttt{FROGGraphRAGState} object as input
    \item Process it according to their specialized functionality
    \item Return the modified state object
\end{enumerate}

\subsubsection{Translation Node}
The Translation Node serves as the entry point for our workflow, detecting the language of user questions and translating non-English questions to English if needed. The complete implementation is provided in \hyperref[code:translation-node]{Appendix~\ref{sec:implementation-code}}.

The implementation integrates Google's Translation API via the \texttt{googletrans} library, providing support for over 100 languages. The node also includes comprehensive event logging for process visualization.

\subsubsection{Entity Extraction Node}
The Entity Extraction Node analyzes the (translated) natural language question to identify relevant entities and properties. The complete implementation is provided in \hyperref[code:entity-extraction-node]{Appendix~\ref{sec:implementation-code}}.

The implementation integrates with the \texttt{llm\_factory} to access fine-tuned models specifically optimized for entity and property extraction tasks. The node handles model-specific formatting, including chat templates for models that support them, and implements robust error handling to ensure the workflow continues even if extraction fails.

\subsubsection{Strategy Selection Node}
The Strategy Selection Node analyzes the question complexity and extracted entities to determine the optimal processing approach. The complete implementation is provided in \hyperref[code:strategy-selection-node]{Appendix~\ref{sec:implementation-code}}.

The strategy selection implements a heuristic approach to determine whether verbalization (direct property lookup) or SPARQL generation is more appropriate for a given question. This decision is based on linguistic analysis of the question text and user settings. The node sets the \texttt{next} field in the state object, which is used by the conditional routing logic in the LangGraph framework.

\begin{figure}
    \centering
    \includegraphics[width=0.6\linewidth]{assets/pics/strategy_selection_4.jpg}
    \caption{Strategy Selection Decision Tree}
\end{figure}

\subsubsection{Property Generation Node}
The Property Generation Node enhances the list of candidate properties using vector search capabilities and semantic relationships:

The Property Generation Node implements a sophisticated property enhancement process that combines n-gram processing, vector search, and domain-specific formatting. The complete implementation is provided in \hyperref[code:property-generation-node]{Appendix~\ref{sec:implementation-code}}.

The Property Generation Node implements a sophisticated property enhancement process that combines:
\begin{enumerate}
    \item N-gram processing to break down the question into searchable chunks
    \item Vector search to find semantically similar properties
    \item Camel case processing to handle different property naming conventions
    \item Domain-specific formatting based on the knowledge source
\end{enumerate}

This approach ensures that even if the entity extraction model misses some properties, the system can still identify relevant properties for SPARQL query generation.

\subsubsection{Verbalization Node}
The Verbalization Node implements direct property retrieval for simple questions, bypassing the need for SPARQL generation. This node is particularly complex as it handles direct graph traversal and result formatting:

The Verbalization Node implements direct property retrieval for simple questions, bypassing the need for SPARQL generation. This node handles direct graph traversal and result formatting. The complete implementation is provided in \hyperref[code:verbalization-node]{Appendix~\ref{sec:implementation-code}}.

The Verbalization Node implements a sophisticated approach that:
\begin{enumerate}
    \item Retrieves candidate entity URIs from the knowledge source API
    \item Uses an LLM to select the most appropriate entity URI based on question context
    \item Fetches all property-object pairs for the selected entity
    \item Computes semantic similarity between property candidates and the question
    \item Selects the highest-similarity property and retrieves its values
    \item Formats the results for direct answer generation, including reference information
\end{enumerate}

This node enables efficient handling of simple questions without requiring SPARQL generation, significantly improving system responsiveness for common query types.

\subsubsection{SPARQL Generation Node}
The SPARQL Generation Node is responsible for generating executable SPARQL queries based on extracted entities and properties, and executing them against the appropriate knowledge graph. The complete implementation is provided in \hyperref[code:sparql-generation-node]{Appendix~\ref{sec:implementation-code}}.

The SPARQL Generation Node implements a sophisticated retry mechanism that attempts to generate and execute valid SPARQL queries up to a configurable threshold. The node:

\begin{enumerate}
    \item Uses knowledge graph metadata to construct appropriate system prompts for different sources
    \item Gathers entity and property resources to provide context for the LLM
    \item Formats prompts based on model-specific requirements
    \item Implements a multi-attempt generation strategy with feedback
    \item Enhances queries with reference information when requested
    \item Executes generated queries and validates results
    \item Updates the state with appropriate context for answer generation
    \item Provides detailed logging for visualization and debugging
\end{enumerate}

For optimal performance, particularly with large knowledge graphs like GESIS, we implement query optimization techniques including:

\begin{itemize}
    \item \textbf{Triple Pattern Ordering}: Arranging triple patterns to minimize intermediate results
    \item \textbf{Query Structure Optimization}: Using nested subqueries for efficient operations
    \item \textbf{Explicit Type Constraints}: Adding type filters to reduce result sets
    \item \textbf{Result Limit Control}: Adding LIMIT clauses to prevent excessive result sizes
\end{itemize}

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{assets//pics/sparql_generation_4.jpg}
    \caption{SPARQL Generation Retry Mechanism}
\end{figure}

\subsubsection{Google Search Node}
The Google Search Node provides a fallback mechanism when knowledge graph approaches fail to provide sufficient results:

The Google Search Node provides a fallback mechanism when knowledge graph approaches fail to provide sufficient results. The complete implementation is provided in \hyperref[code:google-search-node]{Appendix~\ref{sec:implementation-code}}.

The Google Search Node implements a fallback mechanism that leverages Google's search capabilities through the Gemini API. This approach:
\begin{enumerate}
    \item Uses the Gemini API's built-in search capability to retrieve relevant information
    \item Processes search results and resolves proxy URLs to their final destinations
    \item Extracts citation information to provide source attribution
    \item Formats the search results as context for answer generation
\end{enumerate}

This integration provides a robust fallback mechanism when knowledge graph approaches fail, ensuring that users still receive useful information even when the knowledge graph does not contain the requested information.

\subsubsection{Answer Generation Node}
The Answer Generation Node synthesizes the final natural language answer based on the context provided by previous agents:

The Answer Generation Node synthesizes the final natural language answer based on the context provided by previous agents. The complete implementation is provided in \hyperref[code:answer-generation-node]{Appendix~\ref{sec:implementation-code}}.

The Answer Generation Node implements the final processing step that:
\begin{enumerate}
    \item Takes the context string prepared by previous nodes
    \item Creates a prompt instructing the LLM to generate a concise answer
    \item Handles multilingual responses when the original question is in a non-English language
    \item Formats the prompt based on model-specific requirements
    \item Generates the final natural language answer
\end{enumerate}

This node serves as the final output layer of the multi-agent system, transforming the accumulated information into a concise, user-friendly response.

\subsection{Multi-LLM Factory Integration}
A key innovation in our multi-agent implementation is the integration of a flexible LLM factory that supports multiple provider models. This approach enables us to use different specialized models for different tasks, optimizing the performance of each agent node.

A key innovation in our multi-agent implementation is the integration of a flexible LLM factory that supports multiple provider models. The complete implementation is provided in \hyperref[code:llm-factory-integration]{Appendix~\ref{sec:implementation-code}}.

The LLM factory integration offers several key advantages:

\begin{enumerate}
    \item \textbf{Model Specialization}: Different tasks can use different models optimized for their specific requirements. For example, entity extraction might use a model with strong parsing capabilities, while SPARQL generation might use a model with strong reasoning and code generation abilities.
    
    \item \textbf{Configuration Flexibility}: The factory loads configurations from a JSON file, allowing easy adjustment of models and parameters without code changes.
    
    \item \textbf{Provider Abstraction}: The factory abstracts the underlying provider APIs, enabling seamless switching between different LLM providers like Gemini and Ollama.
    
    \item \textbf{Fallback Mechanisms}: The factory implements fallback mechanisms to ensure robustness, including the ability to create a default configuration when the configuration file is not available.
\end{enumerate}

The LLM factory is implemented as a separate module with its own provider architecture:

The LLM factory is implemented as a separate module with its own provider architecture. The complete implementation is provided in \hyperref[code:llm-factory-class]{Appendix~\ref{sec:implementation-code}}.

The LLM factory is designed to support multiple provider types, with each provider implementing a common interface defined by the \texttt{BaseLLMProvider} class. The current implementation supports two providers:

\begin{enumerate}
    \item \textbf{Gemini Provider}: For accessing Google's Gemini models via API
    \item \textbf{Ollama Provider}: For running local models via Ollama
\end{enumerate}

\begin{figure}
    \centering
    \includegraphics[width=0.75\linewidth]{assets//pics/llm_factory.jpg}
    \caption{Multi-Provider LLM Factory Architecture}
\end{figure}

Each specialized agent node retrieves its appropriate model from the factory using the designated getter methods. This allows for specialized models to be used for each task, optimizing the performance of the overall system.

\subsection{Runtime Settings and Configuration}
Our implementation includes a comprehensive runtime settings system that allows users to control the behavior of the knowledge graph integration and query processing components:

Our implementation includes a comprehensive runtime settings system that allows users to control the behavior of the knowledge graph integration and query processing components. The complete implementation is provided in \hyperref[code:runtime-settings]{Appendix~\ref{sec:implementation-code}}.

The runtime settings system provides several key capabilities:

\begin{enumerate}
    \item \textbf{Knowledge Source Selection}: Controls which knowledge graph to query
    \item \textbf{Processing Strategy Control}: Enables/disables verbalization and Google Search
    \item \textbf{Translation Support}: Enables/disables automatic translation
    \item \textbf{Reference Inclusion}: Controls whether references are included in results
    \item \textbf{Visualization Detail}: Controls the level of process visualization
    \item \textbf{Retry Threshold}: Controls how many SPARQL generation attempts are made
\end{enumerate}

These settings can be controlled through the frontend interface, allowing users to customize the system's behavior based on their specific requirements. The settings are stored in a shared state object that flows through the entire processing pipeline, ensuring consistent behavior across all components.

\section{System Integration and Communication}
\label{sec:system-integration}

The integration between the different components of the FROG system relies on a robust communication architecture that enables real-time interactions between the frontend, backend, and knowledge graph services. This section details the technical implementation of the backend architecture, frontend integration, and the real-time communication mechanisms that enable seamless data flow throughout the system.

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{assets/pics/febe_1.jpg}
    \caption{System Integration Architecture Overview}
    \label{fig:system-integration}
\end{figure}

\subsection{Backend Architecture}
The backend of the FROG system is implemented using Django, a high-level Python web framework, with additional components for handling WebSocket connections and asynchronous operations. This architecture provides a solid foundation for the multi-agent system while facilitating real-time communication with the frontend.

\subsubsection{Django Core Structure}
The core Django implementation follows a modular design pattern with clear separation of concerns:

\begin{lstlisting}[breaklines=true,language=python, caption=Django Project Structure, label=code:django-project-structure]
wikidata_web/              # Django project settings
├── settings.py           # Configuration settings
├── urls.py               # URL routing
└── asgi.py               # ASGI configuration for WebSockets

chat/                      # Chat application
├── models.py             # Database models
├── views.py              # API endpoint handlers
├── urls.py               # Application URL routing
├── consumers.py          # WebSocket consumers
└── routing.py            # WebSocket routing
\end{lstlisting}

The Django application is configured with appropriate middleware, installed apps, and database settings. The complete configuration is provided in \hyperref[code:django-settings]{Appendix~\ref{sec:implementation-code}}.

\subsubsection{ASGI and WebSocket Configuration}
The system leverages Django Channels for WebSocket support, configured through the ASGI application. The complete configuration is provided in \hyperref[code:asgi-configuration]{Appendix~\ref{sec:implementation-code}}.

The WebSocket routes are defined in the \texttt{routing.py} file, which maps URL patterns to consumer classes:

\begin{lstlisting}[breaklines=true,language=python, caption=WebSocket Routing Configuration, label=code:websocket-routing]
# backend/chat/routing.py
from django.urls import re_path
from . import consumers

websocket_urlpatterns = [
    re_path(r'^ws/chat/(?P<chat_id>[0-9a-f-]+)/$', consumers.ChatConsumer.as_asgi()),
]
\end{lstlisting}

\subsubsection{REST API Implementation}
The REST API endpoints are implemented using Django REST Framework through a ViewSet that handles all chat-related operations:

\begin{lstlisting}[breaklines=true,language=python, caption=REST API ViewSet Implementation, label=code:rest-api-viewset]
class ChatViewSet(viewsets.ViewSet):
    def list(self, request):
        """Get all chats"""
        chats = Chat.objects.all()
        serializer = ChatListSerializer(chats, many=True)
        return Response(serializer.data)
    
    def retrieve(self, request, pk=None):
        """Get a single chat with its messages"""
        try:
            chat = Chat.objects.get(pk=pk)
            serializer = ChatSerializer(chat)
            return Response(serializer.data)
        except Chat.DoesNotExist:
            return Response({"error": "Chat not found"}, status=status.HTTP_404_NOT_FOUND)
    
    def create(self, request):
        """Create a new chat"""
        chat = Chat.objects.create()
        serializer = ChatListSerializer(chat)
        return Response(serializer.data, status=status.HTTP_201_CREATED)
    
    # Additional API methods...
\end{lstlisting}

The API endpoints support the following operations:
\begin{itemize}
    \item \texttt{GET /api/chats/}: List all chats
    \item \texttt{POST /api/chats/}: Create a new chat
    \item \texttt{GET /api/chats/<uuid>/}: Get a chat with all messages
    \item \texttt{DELETE /api/chats/<uuid>/}: Delete a chat
    \item \texttt{POST /api/chats/<uuid>/send\_message/}: Send a message in a chat
    \item \texttt{GET /api/chats/<uuid>/download\_visualization/}: Download visualization files
\end{itemize}

\subsubsection{Agent Singleton Pattern}
To ensure efficient resource usage, the backend implements a singleton pattern for the agent instance, allowing it to be shared across multiple WebSocket connections:

\begin{lstlisting}[breaklines=true,language=python, caption=Agent Singleton Implementation, label=code:agent-singleton]
# Import the agent singleton instead of the WikidataAgent directly
from agent.singletons import get_agent

# In ChatConsumer.connect
def connect(self):
    # Get API key from environment
    gemini_api_key = os.environ.get('GEMINI_API_KEY')
    
    # Use the agent singleton instead of creating a new instance
    self.agent = get_agent(api_key=gemini_api_key, debug_callback=self.debug_callback)
    logger.info(f"Connected WebSocket for chat_id: {self.chat_id}, using shared agent instance")
\end{lstlisting}

This implementation ensures that system resources are used efficiently while maintaining state consistency across multiple client connections.

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{assets/pics/febe_2.jpg}
    \caption{Backend Architecture and Communication Flow}
    \label{fig:backend-architecture}
\end{figure}

\subsection{Frontend Implementation}
The frontend is built using React with TypeScript, providing a type-safe and component-based architecture. The implementation follows modern React practices with context-based state management and functional components.
\subsubsection{User Interface Implementation}

The frontend implementation provides an intuitive chat-based interface for interacting with the FROG system. The interface is designed to facilitate natural language queries while providing comprehensive feedback about the system's processing steps.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1\linewidth]{assets/pics/homepage.png}
    \caption{FROG System Homepage Interface showing the main chat interface, sidebar navigation, and settings panel. The interface provides users with options to configure knowledge sources, enable/disable system features, and access chat history.}
    \label{fig:homepage-interface}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1\linewidth]{assets/pics/chat.png}
    \caption{Example chat interaction demonstrating the FROG system processing a natural language question. The interface shows real-time feedback from each agent node (indicated by emojis), the generated SPARQL query, execution results, and the final natural language answer with source references.}
    \label{fig:chat-example}
\end{figure}

The user interface successfully integrates all the backend components discussed in the previous sections, providing a seamless experience for users to interact with the multi-agent knowledge graph querying system.

\subsubsection{React Component Structure}
The frontend is organized in a hierarchical component structure that enhances maintainability and reusability:

\begin{lstlisting}[breaklines=true,language=typescript, caption=React Component Structure, label=code:react-component-structure]
frontend/src/
├── components/           # React components
│   ├── ChatArea.tsx      # Main chat display area
│   ├── ChatMessage.tsx   # Individual message rendering
│   ├── Header.tsx        # Application header
│   ├── MessageInput.tsx  # Message input component
│   ├── Settings.tsx      # Settings modal
│   ├── SideNav.tsx       # Sidebar navigation
│   └── SystemMessageGroup.tsx  # Grouped system messages
├── context/              # Context providers
│   └── ChatContext.tsx   # Chat state management
├── types/                # TypeScript interfaces
│   └── index.ts          # Type definitions
└── utils/                # Utility functions
    ├── api.ts            # API client functions
    └── sparqlFormatter.ts  # SPARQL formatting utilities
\end{lstlisting}

The component architecture implements a unidirectional data flow, where state changes propagate downward through the component tree, while user interactions trigger events that are handled by the context provider.

\subsubsection{Context-Based State Management}
The application uses the React Context API for state management, implemented through the \texttt{ChatContext} provider. The complete implementation is provided in \hyperref[code:context-state-management]{Appendix~\ref{sec:implementation-code}}.

This implementation provides centralized state management without the need for additional state management libraries, making the code more maintainable and reducing dependencies.

\subsubsection{Type-Safe API Client}
The frontend communicates with the backend through a type-safe API client that provides strongly-typed functions for interacting with the REST API:

\begin{lstlisting}[breaklines=true,language=typescript, caption=Type-Safe API Client Implementation, label=code:type-safe-api-client]
// frontend/src/utils/api.ts
export const fetchChats = async (): Promise<Chat[]> => {
  const response = await fetch(`${API_BASE_URL}/chats/`, {
    method: 'GET',
    headers: getHeaders(),
  });
  if (!response.ok) {
    throw new Error(`Failed to fetch chats: ${response.status} ${response.statusText}`);
  }
  return response.json();
};

export const sendMessage = async (chatId: string, message: string, settings: AgentSettings): Promise<{ status: string }> => {
  const response = await fetch(`${API_BASE_URL}/chats/${chatId}/send_message/`, {
    method: 'POST',
    headers: getHeaders(),
    body: JSON.stringify({
      message,
      settings
    }),
  });
  if (!response.ok) {
    throw new Error(`Failed to send message: ${response.status} ${response.statusText}`);
  }
  return response.json();
};
\end{lstlisting}

TypeScript interfaces ensure that the data structures passed between the frontend and backend are consistent and properly validated:

\begin{lstlisting}[breaklines=true,language=typescript, caption=TypeScript Interface Definitions, label=code:typescript-interfaces]
// frontend/src/types/index.ts
export interface Chat {
  id: string;
  title: string;
  created_at: string;
  updated_at: string;
  last_message?: Message;
}

export interface Message {
  id: string;
  role: "user" | "assistant" | "system";
  content: string;
  created_at: string;
  visualization_files?: VisualizationFilesContent;
}

export interface AgentSettings {
  useVerbalization: boolean;
  useGoogleSearch: boolean;
  useTranslation: boolean;
  knowledgeSource: "wikidata" | "curriculum" | "legal" | "gesis";
}
\end{lstlisting}

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{assets/pics/react_comp.jpg}
    \caption{Frontend Component Architecture and Data Flow}
    \label{fig:frontend-architecture}
\end{figure}

\subsection{Real-time Communication}
The real-time communication between the frontend and backend is a critical component of the system, enabling instantaneous updates as the multi-agent system processes queries and generates responses. This is implemented through a combination of WebSockets and the Pusher service.

\subsubsection{WebSocket Consumer Implementation}
The core of the real-time communication is the \texttt{ChatConsumer} class, which handles WebSocket connections for individual chat sessions. The complete implementation is provided in \hyperref[code:websocket-consumer]{Appendix~\ref{sec:implementation-code}}.

This implementation uses Django Channels' \texttt{AsyncWebsocketConsumer} as the base class, providing asynchronous handling of WebSocket connections. The consumer is responsible for:

\begin{enumerate}
    \item Managing the WebSocket connection lifecycle
    \item Creating and joining channel groups for group communication
    \item Initializing the agent instance with appropriate callbacks
    \item Processing incoming messages and broadcasting responses
\end{enumerate}

\subsubsection{Real-time Message Processing}
The consumer implements a \texttt{receive} method that processes incoming messages and initiates the agent's query processing:

The consumer implements a \texttt{receive} method that processes incoming messages and initiates the agent's query processing. The complete implementation is provided in \hyperref[code:real-time-message-processing]{Appendix~\ref{sec:implementation-code}}.

This method demonstrates the integration between the WebSocket consumer and the agent system:

\begin{enumerate}
    \item The consumer receives a message over the WebSocket
    \item It immediately sends a "thinking" indicator to the client
    \item The message is saved to the database
    \item The agent's query method is executed in a separate thread using \texttt{run\_in\_executor}
    \item The response is processed, including any visualization files
    \item The result is broadcast to all clients in the channel group
\end{enumerate}

\subsubsection{Debug Callback Implementation}
A key feature of the real-time communication is the debug callback mechanism, which provides step-by-step feedback as the agent processes a query:

A key feature of the real-time communication is the debug callback mechanism, which provides step-by-step feedback as the agent processes a query. The complete implementation is provided in \hyperref[code:debug-callback]{Appendix~\ref{sec:implementation-code}}.
        "Google Search Node": "...",
        # Default emoji for any unrecognized nodes
        "default": "..."
    }
    
    # Extract the node name from the output if possible
    node_name = None
    for key in node_emojis:
        if key in output:
            node_name = key
            break
    
    # Use the appropriate emoji
    emoji = node_emojis.get(node_name, node_emojis["default"])
    
    # Format the message with the emoji
    decorated_output = f"{emoji} {output}"
    
    # Send immediately without buffering
    try:
        await self.send(text_data=json.dumps({
            'debug': decorated_output,
            'role': 'system',
            'message_id': f"debug_{self.message_counter}",
            'timestamp': time.time()  # Add timestamp for debugging
        }))
        
        # Also send to the group for consistency
        await self.channel_layer.group_send(
            self.room_group_name,
            {
                'type': 'debug_message',
                'message': decorated_output,
                'message_id': f"debug_{self.message_counter}",
                'immediate': True  # Flag for immediate processing
            }
        )
        self.message_counter += 1
        
        # Force flush any potential buffers
        await asyncio.sleep(0)  # Yield control to ensure message is sent
        
    except Exception as e:
        logger.error(f"Error sending debug message: {e}")
\end{lstlisting}

This callback function is passed to the agent when initializing, allowing it to send real-time feedback to the client as it processes a query. This provides transparency into the agent's reasoning process and enhances the user experience by showing progress during longer operations.

\subsubsection{Frontend WebSocket Integration}
On the frontend, WebSocket communication is handled through the Pusher service, which provides a consistent interface for real-time updates:

\begin{lstlisting}[breaklines=true,language=typescript, caption=Frontend Pusher Service Implementation, label=code:frontend-pusher-service]
export class PusherService {
  private pusher: Pusher;
  private currentChannel: any = null;
  private isConnected: boolean = false;
  private pendingSubscription: string | null = null;

  constructor() {
    // Disable Pusher logging for production
    Pusher.logToConsole = false;

    this.pusher = new Pusher(PUSHER_CONFIG.key, {
      cluster: PUSHER_CONFIG.cluster,
      forceTLS: PUSHER_CONFIG.forceTLS,
      enabledTransports: ['ws', 'wss'],
    });

    // Connection state handlers
    this.pusher.connection.bind('connected', () => {
      this.isConnected = true;
      
      // Handle pending subscription if exists
      if (this.pendingSubscription) {
        const pendingChatId = this.pendingSubscription;
        this.pendingSubscription = null;
        setTimeout(() => this.handleDelayedSubscription(pendingChatId), 100);
      }
    });

    // Additional connection handlers...
  }

  subscribeToChat(chatId: string, callbacks: {
    onMessage?: (data: PusherMessage) => void;
    onDebugMessage?: (data: PusherMessage) => void;
    onSystemMessage?: (data: PusherMessage) => void;
    onChatMessage?: (data: PusherMessage) => void;
  }) {
    // Subscription implementation...
  }

  // Additional methods...
}
\end{lstlisting}

The frontend components interact with this service through the \texttt{ChatContext}, which handles the processing of real-time messages:

\begin{lstlisting}[breaklines=true,language=typescript, caption=Message Processing in ChatContext, label=code:message-processing-chat-context]
const handlePusherMessage = (data: PusherMessage) => {
  const messageId = data.message_id || `fallback-${Date.now()}-${Math.random()}`;

  // Skip if we've already processed this message
  if (processedMessageIds.has(messageId)) {
    return;
  }

  // Add to processed message set
  processedMessageIds.add(messageId);

  // Handle debug message (system message with debug content)
  if (data.debug) {
    const newMessage: Message = {
      id: messageId,
      role: "system",
      content: data.debug,
      created_at: new Date().toISOString(),
    };

    setCurrentChat((prev) => {
      if (!prev) {
        return null;
      }
      const updatedChat = {
        ...prev,
        messages: [...prev.messages, newMessage],
      };

      // Force immediate scroll for real-time feedback
      setTimeout(() => {
        const messagesEndElement = document.getElementById("messages-end");
        if (messagesEndElement) {
          messagesEndElement.scrollIntoView({ behavior: "smooth" });
        }
      }, 0);

      return updatedChat;
    });
    return;
  }
  
  // Handle regular message with possible visualization files
  if (data.role && data.message) {
    // Process message...
  }
};
\end{lstlisting}

This message processing logic handles different types of messages:
\begin{itemize}
    \item Debug messages from the agent's processing pipeline
    \item Regular chat messages from users and the assistant
    \item System messages for status updates
    \item Special messages containing visualization file references
\end{itemize}

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{assets/pics/febe_4.jpg}
    \caption{Real-time Communication Flow Between Frontend and Backend}
    \label{fig:realtime-communication}
\end{figure}

\subsubsection{Deduplication and Message Ordering}
To handle potential duplicate messages and ensure proper ordering, the system implements a deduplication mechanism using message IDs:

To handle potential duplicate messages and ensure proper ordering, the system implements a deduplication mechanism using message IDs. The complete implementation is provided in \hyperref[code:message-deduplication]{Appendix~\ref{sec:implementation-code}}.

This implementation ensures that messages are only processed once, even if they are received multiple times through different channels (WebSocket, REST API, etc.), maintaining a consistent user experience.

\subsubsection{Configuration-Based Communication}
The communication system is designed to be configurable, with separate configuration for development and production environments:

The communication system is designed to be configurable, with separate configuration for development and production environments. The complete implementation is provided in \hyperref[code:api-configuration]{Appendix~\ref{sec:implementation-code}}.

This approach allows developers to easily switch between different environments by setting environment variables or modifying configuration files, facilitating testing and deployment.

Through this comprehensive implementation of backend architecture, frontend integration, and real-time communication, the FROG system achieves seamless integration between its various components, enabling efficient processing of natural language queries and delivery of results to users in real-time.

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{assets/pics/febe_5.png}
    \caption{End-to-End Data Flow in the FROG System}
    \label{fig:data-flow-diagram}
\end{figure}

\section{Visualization and Logging System}
The visualization and logging system represents a critical component of our FROGGraphRAG framework, enabling comprehensive analysis of the multi-agent execution process through semantic logging and real-time visualization. This section details the technical implementation of our visualization architecture, focusing on two key aspects: RDF-based logging for semantic analysis and real-time process visualization.

\subsection{RDF-Based Logging Architecture}
To enable advanced semantic analysis of agent execution patterns, we implemented a sophisticated RDF-based logging system that captures detailed execution traces in a machine-readable, queryable format. The system follows the SLOGERT (Semantic Log Event Rule Templates) approach, which transforms execution logs into a knowledge graph using standardized ontologies.

\subsubsection{LogToRDF Converter Implementation}
The core of our RDF-based logging is the \texttt{LogToRDF} class, which transforms execution logs into structured RDF triples. The complete implementation is provided in \hyperref[code:log-to-rdf-events]{Appendix~\ref{sec:implementation-code}}.

\subsubsection{RDF Schema Design}
Our RDF schema design follows the W3C PROV Ontology for representing provenance information, combined with custom extensions for agent-specific execution patterns. The schema includes:

\begin{itemize}
    \item \textbf{Event Entities}: Representing individual execution events in the agent workflow
    \item \textbf{Process Nodes}: Representing the specialized agent nodes (Translation, Entity Extraction, etc.)
    \item \textbf{Properties and Parameters}: Capturing the inputs and outputs of each processing step
    \item \textbf{Execution Metadata}: Including timestamps, durations, and approach information
    \item \textbf{Entities and References}: Tracking Wikidata entities and properties referenced during execution
\end{itemize}

This schema design enables sophisticated querying and analysis of execution patterns, component performance, and knowledge graph usage.

\subsubsection{JenaUploader Implementation}
To enable persistent storage and querying of execution logs, we implemented a \texttt{JenaUploader} class that uploads the RDF data to an Apache Jena Fuseki server:

To enable persistent storage and querying of execution logs, we implemented a \texttt{JenaUploader} class that uploads the RDF data to an Apache Jena Fuseki server. The complete implementation is provided in \hyperref[code:jena-uploader]{Appendix~\ref{sec:implementation-code}}.

\subsubsection{Example TTL Output}
Below is an example snippet of TTL output generated by our logging system, showing the RDF representation of a query execution:

Below is an example snippet of TTL output generated by our logging system, showing the RDF representation of a query execution. The complete example is provided in \hyperref[code:ttl-example]{Appendix~\ref{sec:implementation-code}}.

logs:run_20250606121358_a8f69bcd a logex:ConversionMetadata ;
    log:approach "SPARQL Generation" ;
    logex:endTime "2025-06-06T12:14:32.541289"^^xsd:dateTime ;
    logex:hasEventType logid:EventType_sparql_generation_start,
        logid:EventType_start ;
    logex:hasNode logid:Entity_Extraction_Node_Node,
        logid:SPARQL_Generation_Node_Node,
        logid:Translation_Node_Node ;
    logex:startTime "2025-06-06T12:13:58.324156"^^xsd:dateTime ;
    logex:totalDuration 34.217133 ;
    logex:totalEvents 42 .

logs:Event_b7a5d1c2 a log:Event ;
    log:eventType logid:EventType_executing_query_from_attempt_1 ;
    log:hasQuery logs:Query_b7a5d1c2 ;
    log:pname logid:SPARQL_Generation_Node ;
    log:time "2025-06-06T12:14:12.845903"^^xsd:dateTime ;
    logex:belongsToRun logs:run_20250606121358_a8f69bcd .

logs:Query_b7a5d1c2 a logex:SPARQLQuery ;
    log:referencesEntity logs:WikidataEntity_Q5 ;
    log:referencesProperty logs:WikidataProperty_P31,
        logs:WikidataProperty_P106 ;
    rdfs:label "PREFIX wd: <http://www.wikidata.org/entity/>\nPREFIX wdt: <http://www.wikidata.org/prop/direct/>\nPREFIX p: <http://www.wikidata.org/prop/>\nPREFIX ps: <http://www.wikidata.org/prop/statement/>\nPREFIX pr: <http://www.wikidata.org/prop/reference/>\nPREFIX prov: <http://www.w3.org/ns/prov#>\nPREFIX wikibase: <http://wikiba.se/ontology#>\nPREFIX bd: <http://www.bigdata.com/rdf#>\nPREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>\nPREFIX ontolex: <http://www.w3.org/ns/lemon/ontolex#>\n\nSELECT DISTINCT ?person ?occupation ?refUrl ?refDate\nWHERE {\n  ?person wdt:P31 wd:Q5 .\n  ?person wdt:P106 ?occupation .\n  \n  OPTIONAL {\n    ?statement prov:wasDerivedFrom ?ref .\n    OPTIONAL { ?ref pr:P854 ?refUrl . }\n    OPTIONAL { ?ref pr:P813 ?refDate . }\n  }\n}\nLIMIT 100" .

logs:WikidataEntity_Q5 a log:WikidataEntity ;
    log:wikidataId "Q5" .

logs:WikidataProperty_P31 a log:WikidataProperty ;
    log:wikidataId "P31" .

logs:WikidataProperty_P106 a log:WikidataProperty ;
    log:wikidataId "P106" .

logid:EventType_executing_query_from_attempt_1 a log:EventType ;
    logex:eventCount 1 ;
    rdfs:label "executing query from attempt 1" .
\end{lstlisting}

\subsection{Real-Time Visualization}
In addition to the RDF-based logging system, we implemented a real-time visualization system that provides immediate feedback on the agent's execution process. This system is particularly useful for debugging and understanding the agent's decision-making process.

\subsubsection{BoxologyVisualizer Implementation}
The core of our visualization system is the \texttt{BoxologyVisualizer} class, which captures execution events and generates Mermaid\footnote{\url{https://mermaid.js.org/}} diagrams representing the process flow. The complete implementation is provided in \hyperref[code:boxology-visualizer]{Appendix~\ref{sec:implementation-code}}.

The visualizer maintains an in-memory log of execution events, capturing detailed information about each step in the agent's processing pipeline. This information includes:

\begin{itemize}
    \item Component/node name
    \item Event type
    \item Timestamp
    \item Event details (inputs, outputs, etc.)
    \item Duration information
\end{itemize}

Events are captured through the \texttt{log\_event} method, which is called by each agent node during execution:

\begin{lstlisting}[language=python, caption=Event logging implementation, label=code:event-logging]
def log_event(self, component, event_type, details=None, start_time=None, end_time=None):
    """Log a process event with timestamp"""
    if self.verbose < 1:
        return
        
    timestamp = datetime.now()
    log_entry = {
        "component": component,
        "event_type": event_type,
        "details": details,
        "timestamp": timestamp.isoformat(),
        "start_time": start_time.isoformat() if start_time else None,
        "end_time": end_time.isoformat() if end_time else None
    }
    
    self.logs.append(log_entry)
    
    # If debug_callback is provided, send the log entry through it
    if self.debug_callback:
        debug_msg = f"{component} - {event_type}"
        if details:
            debug_msg += f"\nDetails: {json.dumps(details, cls=CustomEncoder, indent=2)}"
        
        if start_time and end_time:
            duration = (end_time - start_time).total_seconds()
            debug_msg += f"\nDuration: {duration:.3f}s"
        
        # Handle asynchronous callbacks
        if asyncio.iscoroutinefunction(self.debug_callback):
            try:
                loop = asyncio.get_event_loop()
                if loop.is_running():
                    future = asyncio.run_coroutine_threadsafe(self.debug_callback(debug_msg), loop)
                    # Optionally wait for result with timeout
                    try:
                        future.result(timeout=1.0)
                    except Exception as e:
                        print(f"Error in async callback: {e}")
                else:
                    loop.run_until_complete(self.debug_callback(debug_msg))
            except RuntimeError:
                # No event loop in this thread, create a new one
                loop = asyncio.new_event_loop()
                asyncio.set_event_loop(loop)
                loop.run_until_complete(self.debug_callback(debug_msg))
        else:
            # Regular synchronous callback
            try:
                self.debug_callback(debug_msg)
            except Exception as e:
                print(f"Error in callback: {e}")
\end{lstlisting}

\subsubsection{Mermaid Diagram Generation}
A key feature of the visualization system is the generation of Mermaid diagrams, which provide a visual representation of the agent's execution process. The diagrams are generated by the \texttt{save\_mermaid\_diagram} method:

A key feature of the visualization system is the generation of Mermaid diagrams, which provide a visual representation of the agent's execution process. The complete implementation is provided in \hyperref[code:mermaid-generation]{Appendix~\ref{sec:implementation-code}}.

\subsubsection{Example Mermaid Diagram}
Below is an example Mermaid diagram generated by our visualization system:

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{mermaid_vissss.jpg}
    \caption{Example Mermaid visualization of the agent execution process (\textcolor{blue}{\href{https://tinyurl.com/mermaid-visualization}{Click here for clear version}})}
    \label{fig:mermaid-example}
\end{figure}

It's important to note that Mermaid diagrams have inherent limitations due to their programmatic rendering nature. The diagrams often render horizontally too wide, especially when visualizing complex workflows with multiple components and detailed event sequences. This horizontal expansion makes it necessary for users to zoom in or scroll horizontally to read the content properly. Despite this limitation, Mermaid diagrams remain valuable for their ability to programmatically generate visualizations from execution logs, providing a standardized view of the agent's processing pipeline that would be difficult to create manually.

\subsubsection{JSON Log Export}
In addition to generating Mermaid diagrams and RDF representations, the visualization system also exports the complete execution logs in JSON format for easier analysis and debugging:

In addition to generating Mermaid diagrams and RDF representations, the visualization system also exports the complete execution logs in JSON format for easier analysis and debugging. The complete implementation is provided in \hyperref[code:json-export]{Appendix~\ref{sec:implementation-code}}.

The JSON export contains the complete, structured record of all execution events, including:
\begin{itemize}
    \item The original question and approach used
    \item Start time, end time, and total duration
    \item Complete chronological log of all events from all components
    \item Detailed event information including component, event type, timestamp, and full details
    \item Duration information for timed events
\end{itemize}

Unlike the Mermaid diagram (which provides a visual representation) and the TTL format (which enables semantic querying), the JSON format provides a complete, machine-readable record of the execution process that can be easily parsed and analyzed using standard tools. This is particularly useful for offline analysis, debugging, and performance monitoring.

\begin{lstlisting}[language=json, caption=Example JSON log excerpt, label=code:json-example]
{
  "question": "Who is the CEO of Tencent?",
  "approach_used": "Verbalization",
  "start_time": "2025-06-06T14:25:32.456123",
  "end_time": "2025-06-06T14:25:33.891234",
  "total_duration": 1.435111,
  "logs": [
    {
      "component": "Translation Node",
      "event_type": "start",
      "details": {"question": "who is the CEO of Tencent?"},
      "timestamp": "2025-06-06T14:25:32.456123",
      "start_time": "2025-06-06T14:25:32.456123",
      "end_time": null
    },
    {
      "component": "Translation Node",
      "event_type": "language detection",
      "details": {"detected_language": "en", "translation_enabled": true},
      "timestamp": "2025-06-06T14:25:32.489456",
      "start_time": null,
      "end_time": null
    }
  ]
}
\end{lstlisting}

The JSON export is generated alongside the Mermaid diagram and TTL file when the visualization system's \texttt{save\_visualization\_files} method is called at the end of the query process.

\subsubsection{Debug Callback System}
To enable real-time feedback during the agent's execution, we implemented a debug callback system that sends event notifications to connected clients. This system is integrated with the Django Channels WebSocket framework:

\begin{lstlisting}[language=python, caption=Debug callback implementation, label=code:debug-callback]
async def debug_callback(self, output):
    """Callback function for agent debugging output with real-time streaming"""
    # Fun emojis for each node type
    node_emojis = {
        "Translation Node": "...",
        "Entity Extraction Node": "...",
        "Strategy Selection Node": "...",
        "Verbalization Node": "...", 
        "Property Generation Node": "...",
        "SPARQL Generation Node": "...",
        "Answer Generation Node": "...",
        "Google Search Node": "...",
        # Default emoji for any unrecognized nodes
        "default": "..."
    }
    
    # Extract the node name from the output if possible
    node_name = None
    for key in node_emojis:
        if key in output:
            node_name = key
            break
    
    # Use the appropriate emoji
    emoji = node_emojis.get(node_name, node_emojis["default"])
    
    # Format the message with the emoji
    decorated_output = f"{emoji} {output}"
    
    # Send immediately without buffering
    try:
        await self.send(text_data=json.dumps({
            'debug': decorated_output,
            'role': 'system',
            'message_id': f"debug_{self.message_counter}",
            'timestamp': time.time()  # Add timestamp for debugging
        }))
        
        # Also send to the group for consistency
        await self.channel_layer.group_send(
            self.room_group_name,
            {
                'type': 'debug_message',
                'message': decorated_output,
                'message_id': f"debug_{self.message_counter}",
                'immediate': True  # Flag for immediate processing
            }
        )
        self.message_counter += 1
        
        # Force flush any potential buffers
        await asyncio.sleep(0)  # Yield control to ensure message is sent
        
    except Exception as e:
        logger.error(f"Error sending debug message: {e}")
\end{lstlisting}

The debug callback system provides real-time feedback about the agent's execution process, allowing users to see exactly what is happening at each step. This is particularly useful for diagnosing issues and understanding the agent's decision-making process.

\subsection{Apache Jena Integration for Semantic Analysis}
The RDF-based logging system is integrated with Apache Jena Fuseki, a semantic web server that enables sophisticated SPARQL querying of execution logs. This integration allows for advanced analysis of execution patterns, component performance, and knowledge graph usage.

\subsubsection{SPARQL Query Results and Dashboard}
The Apache Jena Fuseki server provides a web-based interface for executing SPARQL queries and visualizing the results. The results can be exported in various formats (JSON, CSV, XML, etc.) for further analysis or visualization.

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{assets/pics/jena_dashboard.png}
    \caption{Apache Jena Fuseki dashboard for SPARQL query execution and visualization}
    \label{fig:jena-dashboard}
\end{figure}

\subsubsection{SPARQL Query Examples}
Our Apache Jena integration enables a wide range of analytical queries on the execution logs. Below are some example queries that demonstrate the power of the semantic logging approach:

\begin{lstlisting}[language=sparql, caption=SPARQL query for listing all runs with timestamps, label=code:sparql-runs]
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX logex: <https://w3id.org/sepses/ns/logex#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>

SELECT ?run ?startTime ?endTime ?duration ?totalEvents
WHERE {
  ?run rdf:type logex:ConversionMetadata ;
       logex:startTime ?startTime ;
       logex:endTime ?endTime ;
       logex:totalDuration ?duration ;
       logex:totalEvents ?totalEvents .
} 
ORDER BY DESC(?startTime)
\end{lstlisting}

\begin{lstlisting}[language=sparql, caption=SPARQL query for finding runs with specific entities, label=code:sparql-entities]
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX log: <https://w3id.org/sepses/ns/log#>
PREFIX logex: <https://w3id.org/sepses/ns/logex#>

SELECT DISTINCT ?run ?entityLabel ?startTime
WHERE {
  ?event log:hasEntity ?entity .
  ?event logex:belongsToRun ?run .
  ?entity rdfs:label ?entityLabel .
  ?run logex:startTime ?startTime .
  
  # Filter for specific entity (remove or change this filter as needed)
  FILTER(CONTAINS(LCASE(?entityLabel), "ma huateng"))
}
ORDER BY DESC(?startTime)
\end{lstlisting}

\begin{lstlisting}[language=sparql, caption=SPARQL query for finding SPARQL queries used in runs, label=code:sparql-queries]
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX log: <https://w3id.org/sepses/ns/log#>
PREFIX logex: <https://w3id.org/sepses/ns/logex#>

SELECT ?run ?queryText ?startTime
WHERE {
  ?event log:hasQuery ?query .
  ?event logex:belongsToRun ?run .
  ?query rdfs:label ?queryText .
  ?run logex:startTime ?startTime .
}
ORDER BY DESC(?startTime)
\end{lstlisting}

\begin{lstlisting}[language=sparql, caption=SPARQL query for analyzing approach distribution, label=code:sparql-approaches]
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX log: <https://w3id.org/sepses/ns/log#>
PREFIX logex: <https://w3id.org/sepses/ns/logex#>

SELECT ?approach (COUNT(?event) as ?count)
WHERE {
  ?event log:approach ?approach .
}
GROUP BY ?approach
ORDER BY DESC(?count)
\end{lstlisting}

\begin{lstlisting}[language=sparql, caption=SPARQL query for calculating average duration by component, label=code:sparql-duration]
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX log: <https://w3id.org/sepses/ns/log#>
PREFIX logid: <https://sepses.ifs.tuwien.ac.at/id/log#>

SELECT ?component (AVG(?duration) as ?avgDuration) (COUNT(?event) as ?count)
WHERE {
  ?event log:pname ?componentUri ;
         log:duration ?duration .
  BIND(STRAFTER(STR(?componentUri), "log#") as ?component)
}
GROUP BY ?component
ORDER BY DESC(?avgDuration)
\end{lstlisting}

\begin{lstlisting}[language=sparql, caption=SPARQL query for finding most common entities, label=code:sparql-common-entities]
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX log: <https://w3id.org/sepses/ns/log#>

SELECT ?entityLabel (COUNT(?entity) as ?count)
WHERE {
  ?event log:hasEntity ?entity .
  ?entity rdfs:label ?entityLabel .
}
GROUP BY ?entityLabel
ORDER BY DESC(?count)
LIMIT 20
\end{lstlisting}

\begin{lstlisting}[language=sparql, caption=SPARQL query for finding most common Wikidata properties, label=code:sparql-common-properties]
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX log: <https://w3id.org/sepses/ns/log#>

SELECT ?propertyId (COUNT(?property) as ?count)
WHERE {
  ?query log:referencesProperty ?property .
  ?property log:wikidataId ?propertyId .
}
GROUP BY ?propertyId
ORDER BY DESC(?count)
LIMIT 20
\end{lstlisting}

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{assets/pics/jena_query_results.png}
    \caption{Apache Jena Fuseki SPARQL query results showing execution statistics}
    \label{fig:jena-results}
\end{figure}

\subsubsection{Analytical Capabilities}
The integration with Apache Jena Fuseki enables a wide range of analytical capabilities:

\begin{itemize}
    \item \textbf{Execution Pattern Analysis}: Identifying common execution paths and decision patterns
    \item \textbf{Performance Monitoring}: Measuring component execution times and identifying bottlenecks
    \item \textbf{Entity and Property Usage}: Tracking the most commonly used Wikidata entities and properties
    \item \textbf{Strategy Distribution}: Analyzing the distribution of verbalization vs. SPARQL generation approaches
    \item \textbf{Error Analysis}: Identifying common error patterns and failure modes
    \item \textbf{Temporal Trends}: Analyzing performance and behavior changes over time
\end{itemize}

These analytical capabilities provide valuable insights into the agent's behavior and performance, enabling continuous improvement and optimization of the system.

\subsection{Integration with Multi-Agent System}
The visualization and logging system is tightly integrated with the multi-agent system described in Section \ref{sec:system-integration}. Each agent node calls the visualizer's \texttt{log\_event} method at key points during execution, providing detailed information about the node's operations.

This integration enables comprehensive monitoring and analysis of the agent's execution process, providing valuable insights into its behavior and performance. The system captures information about:

\begin{itemize}
    \item Translation language detection and processing
    \item Entity and property extraction results
    \item Strategy selection decisions
    \item SPARQL query generation and execution
    \item Verbalization processing
    \item Answer generation
    \item Execution times and performance metrics
\end{itemize}

The tight integration between the visualization system and the multi-agent architecture enables a transparent and explainable execution process, allowing users to understand how the agent arrives at its answers.

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{assets/pics/visualization_system_architecture.jpg}
    \caption{Architecture of the Visualization and Logging System and its integration with the Multi-Agent System}
    \label{fig:vis-architecture}
\end{figure}

Through this comprehensive visualization and logging system, we enable detailed analysis and understanding of the agent's execution process, supporting both debugging and continuous improvement of the system.

\section{Computational Setup}
\label{sec:computational-setup}

This section details the hardware specifications, deployment configurations, and interconnectivity of the various components in our implementation. The computational setup employs a distributed architecture across multiple devices, with services interconnected through both local network and cloud-based solutions.

\subsection{Hardware Environment}
The implementation is distributed across two identical devices, both MacBook Pro M3 models with 18GB unified memory. The distribution of components was designed to balance computational load while ensuring reliable performance:

\begin{itemize}
    \item \textbf{Device 1}: Hosts the Django backend with multi-agent system and Ollama for local model inference
    \item \textbf{Device 2}: Hosts supporting infrastructure including Weaviate vector database and Apache Jena Fuseki
\end{itemize}

\subsection{Frontend Deployment}
The React frontend is deployed through Vercel's continuous deployment pipeline, which is directly connected to the GitHub repository. This setup provides automatic builds and deployments whenever changes are pushed to the web branch:

\begin{itemize}
    \item \textbf{Repository Path}: \texttt{https://github.com/gansixeneh/FROG-2.0/tree/web} in the \texttt{wikidata-chat/frontend} folder
    \item \textbf{Deployment Platform}: Vercel with GitHub integration for continuous deployment
    \item \textbf{Build Configuration}: Automatic builds triggered by commits to the web branch
\end{itemize}

This approach ensures that the frontend is always synchronized with the latest codebase while providing robust hosting and content delivery capabilities.

\subsection{Backend Infrastructure}
The backend implementation runs locally on Device 1 and is made accessible to the Internet through ngrok, which provides a secure tunnel to the locally running Django application.

\begin{itemize}
    \item \textbf{Django Server}: Runs locally on Device 1, providing the REST API and WebSocket endpoints
    \item \textbf{Ngrok Tunnel}: Utilizes ngrok's free tier static domain feature\footnote{\url{https://ngrok.com/blog-post/free-static-domains-ngrok-users}} to provide a consistent public URL for frontend communication
    \item \textbf{Ollama Provider}: Runs locally on Device 1 with the command \texttt{ollama serve}, exposing the API on \texttt{localhost:11434} for local model inference
\end{itemize}

This setup enables the locally running backend to be securely exposed to the Internet without requiring dedicated hosting infrastructure, while the Ollama provider allows local execution of fine-tuned models without relying on external APIs.

\subsection{Pusher Integration}
For real-time communication between the frontend and backend, we utilize Pusher's WebSocket infrastructure. The implementation uses Pusher's free tier, which provides:

\begin{itemize}
    \item 200,000 messages per day
    \item 100 concurrent connections
    \item Real-time event broadcasting and channel subscription
\end{itemize}

Pusher serves as the bridge between the frontend deployed on Vercel and the backend running locally behind ngrok, enabling real-time updates during the multi-agent execution process.\footnote{\url{https://pusher.com/channels/pricing/}}

The decision to use Pusher as an intermediary WebSocket service was necessitated by Vercel's stateless serverless architecture, which limits WebSocket connections to approximately one minute before timing out. Since our multi-agent system often requires longer processing times for complex queries, a direct WebSocket connection through Vercel would be insufficient. Pusher resolves this limitation by providing a persistent connection infrastructure that bridges the stateless Vercel frontend with our long-running backend processes.

\subsection{Weaviate Vector Database}
The Weaviate vector database is deployed on Device 2 using Docker Compose with the following configuration:

\begin{lstlisting}[breaklines=true,language=yaml, caption=Weaviate Docker Compose Configuration, label=code:weaviate-docker]
services:
  weaviate:
    image: semitechnologies/weaviate:latest
    ports:
      - "${WEAVIATE_HTTP_PORT:-8080}:8080"
      - "${WEAVIATE_GRPC_PORT:-50052}:50051"
    environment:
      QUERY_DEFAULTS_LIMIT: 20
      AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED: 'true'
      PERSISTENCE_DATA_PATH: '/var/lib/weaviate'
      DEFAULT_VECTORIZER_MODULE: 'none'
      ENABLE_MODULES: ''
      CLUSTER_HOSTNAME: 'node1'
    volumes:
      - weaviate_data:/var/lib/weaviate
volumes:
  weaviate_data:
\end{lstlisting}

The configuration exposes Weaviate on the default ports (8080 for HTTP and 50052 for gRPC) and enables persistent storage through a Docker volume. The backend on Device 1 connects to Weaviate on Device 2 using the local network IP address within the same WiFi network.

\subsection{Apache Jena Fuseki}
The Apache Jena Fuseki semantic web server is also deployed on Device 2, running on its default port (3030). This server provides SPARQL query capabilities for the RDF-based logging system, enabling sophisticated analysis of agent execution patterns.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1\linewidth]{assets/pics/diagram_spam_logo.jpg}
    \caption{Component Connectivity Diagram Showing Communication Paths}
    \label{fig:connectivity-diagram}
\end{figure}

\subsection{Cross-Device Communication}
Communication between Device 1 and Device 2 occurs through the local network, with the backend on Device 1 connecting to services on Device 2 using its local IP address. This approach ensures low-latency communication between the backend and supporting infrastructure while avoiding potential security issues associated with exposing these services to the Internet.

The distributed architecture balances computational load while ensuring reliable performance, with the Django backend and Ollama provider running on Device 1, and the supporting infrastructure (Weaviate and Apache Jena) running on Device 2. This setup enables efficient processing of natural language queries and delivery of results in real-time, while providing comprehensive logging and analysis capabilities.